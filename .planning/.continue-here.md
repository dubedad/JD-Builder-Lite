---
milestone: v2.1-UAT
task: informal fixes
status: in_progress
last_updated: 2026-01-25
---

<current_state>
Working on v2.1 UAT feedback fixes - informal bug fixes based on user testing.
No formal milestone/phase structure yet - operating in "quick fix" mode.

Just completed fixing source_attribute mismatch for Work Activities.
</current_state>

<completed_work>

**This session:**
- Fixed "Create Job Description" button not showing (added .btn styles to main.css)
- Fixed GC CDTS header styling per official format
- Added JobForge logo to welcome section
- Moved Explore modules to appear below search results
- Added provenance description below "Authoritative Sources"
- Implemented hover effect for explore cards (blue bg, white text)
- Added publication date and data steward beside results count
- Fixed Key Activities tab - added SOURCE_ATTRIBUTE_LABELS to enrichment_service.py
  - Issue: `category.title()` converted 'work_activities' to 'Work_Activities' (underscore)
  - Frontend filter expected 'Work Activities' (with space)
  - Added mapping to return correct label

**Prior sessions (v2.0):**
- Complete UI redesign with GC CDTS header/footer
- OaSIS-style welcome layout
- Tab-based profile display
- Statement selection with tooltips
</completed_work>

<remaining_work>

**Pending user verification:**
- Key Activities tab should now show both Main Duties AND Work Activities
- Effort tab should show only Work Context items containing "effort"
- Responsibility tab should show only Work Context items containing "responsi*" or "decision"
  (Backend already configured correctly with CLASSIFICATION_PATTERNS)

**Unaddressed from UAT feedback:**
- Need formal v2.1 milestone planning if more features requested
- User mentioned B.1-B.5, C.1-C.3, C.5, D.1-D.3 pending review items
</remaining_work>

<decisions_made>

- Used SOURCE_ATTRIBUTE_LABELS mapping instead of fixing category naming to preserve backward compatibility
- Kept existing CLASSIFICATION_PATTERNS (["responsib", "decision"] for responsibility, ["effort"] for effort) - already correct per user requirements
- Created handoff at .planning root since no formal v2.1 phase structure exists yet
</decisions_made>

<blockers>
None currently - waiting for user verification of fixes
</blockers>

<context>
User provided UAT feedback via CSV file and screenshots. We've been implementing fixes incrementally without formal GSD phase structure. Key files modified:

- src/services/enrichment_service.py - Added SOURCE_ATTRIBUTE_LABELS mapping
- static/css/main.css - Button styles, GC header, explore cards
- static/js/main.js - Show/hide logic for explore section
- templates/index.html - Complete rewrite with GC CDTS structure

Flask server needs restart to pick up backend changes.
</context>

<next_action>
1. User should restart Flask server and test Key Activities tab
2. Verify Work Activities now appear under Key Activities
3. Verify Effort/Responsibility tabs show correctly filtered Work Context items
4. If more features needed, run `/gsd:new-milestone` to formalize v2.1 planning
</next_action>
