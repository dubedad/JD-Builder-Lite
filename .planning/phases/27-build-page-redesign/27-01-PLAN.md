---
phase: 27-build-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/index.html
  - static/css/main.css
  - static/js/accordion.js
  - static/js/state.js
autonomous: true

must_haves:
  truths:
    - "Occupation header card shows dark navy background with gear icon, title, NOC code badge, lead statement, and metadata row"
    - "Each tab button shows a Font Awesome icon next to its label"
    - "Every tab panel shows a blue-grey section description box at the top before any content"
    - "Header card has close button that returns to search"
    - "Overview tab shows an editable Position Title text input at the top"
  artifacts:
    - path: "templates/index.html"
      provides: "Updated profile-header HTML structure and tab button icons"
      contains: "fa-cog"
    - path: "static/css/main.css"
      provides: "Dark navy header card styles, section description box styles, tab icon spacing, overview position-title input styles"
      contains: "section-description-box"
    - path: "static/js/accordion.js"
      provides: "Rewritten renderProfileHeader, section description box injection in renderTabContent, redesigned renderOverviewContent with Position Title input"
      contains: "section-description-box"
  key_links:
    - from: "static/js/accordion.js"
      to: "templates/index.html"
      via: "renderProfileHeader populates #profile-header elements"
      pattern: "getElementById.*profile-header"
    - from: "static/js/accordion.js"
      to: "static/css/main.css"
      via: "section-description-box class used in renderTabContent"
      pattern: "section-description-box"
    - from: "static/js/accordion.js"
      to: "static/js/state.js"
      via: "Position Title input reads/writes store.getState().positionTitle"
      pattern: "positionTitle"
---

<objective>
Redesign the occupation header card, add icons + section description boxes to all Build page tabs, and redesign the Overview tab content with an editable Position Title field.

Purpose: The current header is a plain blue banner (`blueBG` class) with minimal info. The v5.1 spec requires a dark navy card with gear icon, NOC code, lead statement, metadata badges (NOC version, Retrieved timestamp, Quality Verified), and a close button. Additionally, every tab button needs an FA icon and every tab panel needs a blue-grey description box at the top explaining the tab's contents. The Overview tab needs a Position Title input, two-column Lead Statement + Definition layout, Employment Requirements bullets, and Typical Workplaces pills.

Output: Updated header card HTML/CSS, tab button icons in index.html, section description box CSS, JS that injects description boxes into each tab panel during render, and redesigned Overview tab with Position Title input.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-build-page-redesign/27-RESEARCH.md
@.planning/phases/27-build-page-redesign/27-CONTEXT.md
@templates/index.html
@static/js/accordion.js
@static/js/state.js
@static/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign occupation header card (HTML + CSS + JS)</name>
  <files>templates/index.html, static/css/main.css, static/js/accordion.js</files>
  <action>
**HTML (templates/index.html):** Replace the existing `#profile-header` section (lines 158-176) with a new dark navy header card structure:

```html
<section id="profile-header" class="profile-header profile-header--v51 hidden">
    <button class="profile-header__close" id="profile-header-close" type="button" aria-label="Close profile and return to search">
        <i class="fas fa-times"></i>
    </button>
    <div class="profile-header__content">
        <div class="profile-header__icon-wrap">
            <i id="profile-icon" class="fas fa-cog" aria-hidden="true"></i>
        </div>
        <div class="profile-header__text">
            <h2 id="profile-title" class="profile-header__title"></h2>
            <span id="profile-code-badge" class="profile-header__noc-badge"></span>
            <p id="profile-lead-statement" class="profile-header__lead"></p>
            <p id="profile-description" class="profile-header__description hidden"></p>
        </div>
    </div>
    <div class="profile-header__meta">
        <span class="profile-header__meta-item">
            <i class="fas fa-code-branch"></i> NOC 2021 v1.0
        </span>
        <span class="profile-header__meta-divider">|</span>
        <span class="profile-header__meta-item" id="profile-timestamp">
            <i class="fas fa-clock"></i> Retrieved: ...
        </span>
        <span class="profile-header__meta-divider">|</span>
        <span class="profile-header__meta-item profile-header__meta-item--verified">
            <i class="fas fa-check-circle"></i> Quality Verified
        </span>
        <span class="profile-header__meta-divider">|</span>
        <button class="profile-header__provenance-btn" type="button" onclick="event.preventDefault();">
            <i class="fas fa-project-diagram"></i> View Provenance Graph
        </button>
    </div>
    <div class="profile-header__links">
        <a id="profile-link" href="#" target="_blank" rel="noopener noreferrer" class="profile-header__oasis-link">
            <i class="fas fa-external-link-alt"></i> View on OASIS
        </a>
    </div>
</section>
```

Remove the `blueBG` class reference entirely. The new class `profile-header--v51` handles all styling.

**CSS (static/css/main.css):** Replace the existing `.profile-header` block (around lines 1161-1270) with new v5.1 styles:

```css
/* v5.1 Occupation Header Card */
.profile-header--v51 {
    background: var(--primary); /* #26374a dark navy */
    color: #ffffff;
    border-radius: 8px;
    padding: 1.5rem;
    margin-bottom: 1rem;
    position: relative;
}

.profile-header__close {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    background: none;
    border: none;
    color: rgba(255,255,255,0.7);
    font-size: 1.25rem;
    cursor: pointer;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    transition: color 0.2s, background 0.2s;
}
.profile-header__close:hover {
    color: #ffffff;
    background: rgba(255,255,255,0.15);
}

.profile-header--v51 .profile-header__content {
    display: flex;
    align-items: flex-start;
    gap: 1.25rem;
}

.profile-header__icon-wrap {
    flex-shrink: 0;
    width: 56px;
    height: 56px;
    background: rgba(255,255,255,0.12);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    color: #ffffff;
}

.profile-header--v51 .profile-header__title {
    font-size: 1.35rem;
    font-weight: 700;
    margin-bottom: 0.25rem;
    color: #ffffff;
    padding-right: 2rem; /* space for close button */
}

.profile-header__noc-badge {
    display: inline-block;
    background: rgba(255,255,255,0.18);
    color: #ffffff;
    padding: 0.15rem 0.6rem;
    border-radius: 4px;
    font-size: 0.8rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    font-family: monospace;
}

.profile-header--v51 .profile-header__lead {
    color: rgba(255,255,255,0.88);
    font-size: 0.95rem;
    line-height: 1.5;
    margin-top: 0.5rem;
}

.profile-header--v51 .profile-header__description {
    color: rgba(255,255,255,0.75);
    font-size: 0.88rem;
    margin-top: 0.5rem;
}

.profile-header__meta {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 1rem;
    padding-top: 0.75rem;
    border-top: 1px solid rgba(255,255,255,0.15);
    font-size: 0.8rem;
    color: rgba(255,255,255,0.7);
}

.profile-header__meta-item {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
}
.profile-header__meta-item--verified {
    color: #81c784; /* soft green */
}
.profile-header__meta-divider {
    color: rgba(255,255,255,0.3);
}

.profile-header__provenance-btn {
    background: none;
    border: 1px solid rgba(255,255,255,0.3);
    color: rgba(255,255,255,0.8);
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
    font-size: 0.78rem;
    cursor: pointer;
    transition: background 0.2s, color 0.2s;
}
.profile-header__provenance-btn:hover {
    background: rgba(255,255,255,0.1);
    color: #ffffff;
}

.profile-header__links {
    margin-top: 0.5rem;
}
.profile-header__oasis-link {
    color: rgba(255,255,255,0.7);
    font-size: 0.8rem;
    text-decoration: none;
}
.profile-header__oasis-link:hover {
    color: #ffffff;
    text-decoration: underline;
}

/* Mobile responsive */
@media (max-width: 768px) {
    .profile-header--v51 .profile-header__content {
        flex-direction: column;
        align-items: center;
        text-align: center;
    }
    .profile-header__meta {
        justify-content: center;
    }
}
```

Keep the old `.profile-header` styles as-is but they will be unused since the HTML now uses `profile-header--v51`. The old `.blueBG` class can remain for now (unused).

**JS (static/js/accordion.js):** Update `renderProfileHeader()` (lines 82-127) to populate the new structure:

1. Change the icon element selector to find `#profile-icon` inside the new `.profile-header__icon-wrap`
2. The icon should default to `fa-cog` (gear) as set in HTML. After `getNocIcon()` returns, update it: `iconEl.className = 'fas fa-cog';` — actually, per v5.1 spec, the header always shows a gear icon regardless of NOC category. So remove the `getNocIcon()` call from renderProfileHeader (it stays in renderOverviewContent for the overview tab icon). Set `iconEl.className = 'fas fa-cog';` unconditionally.
3. Update timestamp format: `timestampEl.textContent = 'Retrieved: ' + new Date().toLocaleDateString('en-CA');` (ISO date)
4. The `profile-link` and `profile-code-badge` element IDs remain the same, so existing assignments still work.
5. Add close button handler at end of renderProfileHeader:

```javascript
const closeBtn = document.getElementById('profile-header-close');
if (closeBtn) {
    closeBtn.onclick = () => {
        // Return to search — go back to step 1
        if (window.jdStepper) {
            window.jdStepper.goToStep(1);
        }
    };
}
```
  </action>
  <verify>
Run the app with `PYTHONPATH=. python3 src/app.py`, search for an occupation, click a result card. Verify:
1. Dark navy header card appears with gear icon, title, NOC badge, lead statement
2. Metadata row shows "NOC 2021 v1.0 | Retrieved: YYYY-MM-DD | Quality Verified | View Provenance Graph"
3. Close button (x) in top-right returns to search (step 1)
4. No JS console errors
  </verify>
  <done>Header card renders as dark navy card with gear icon, NOC code badge, lead statement paragraph, metadata badges row, OASIS link, and close button. Old blue banner is fully replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Add tab icons and section description boxes</name>
  <files>templates/index.html, static/css/main.css, static/js/accordion.js</files>
  <action>
**HTML (templates/index.html):** Update each tab button (lines 348-394) to include an FA icon before the label text. The icon goes inside the button element, before the text node:

```
tab-overview:        <i class="fas fa-eye tab-icon"></i> Overview
tab-core-competencies: <i class="fas fa-star tab-icon"></i> Core Competencies
tab-activities:      <i class="fas fa-list-check tab-icon"></i> Key Activities
tab-skills:          <i class="fas fa-lightbulb tab-icon"></i> Skills
tab-abilities:       <i class="fas fa-brain tab-icon"></i> Abilities
tab-knowledge:       <i class="fas fa-book-open tab-icon"></i> Knowledge
tab-effort:          <i class="fas fa-exchange-alt tab-icon"></i> Effort
tab-responsibility:  <i class="fas fa-user tab-icon"></i> Responsibility
```

**CSS (static/css/main.css):** Add styles for tab icons and section description boxes:

```css
/* Tab button icons */
.tab-icon {
    margin-right: 0.4rem;
    font-size: 0.85em;
    opacity: 0.7;
}
.tab-button-active .tab-icon {
    opacity: 1;
}

/* Section Description Box (v5.1) */
.section-description-box {
    background: #f0f4f8;
    border-left: 4px solid var(--primary);
    border-radius: 4px;
    padding: 0.75rem 1rem;
    margin-bottom: 1rem;
    display: flex;
    gap: 0.75rem;
    align-items: flex-start;
    font-size: 0.88rem;
    line-height: 1.5;
    color: var(--text);
}
.section-description-box__icon {
    color: var(--primary);
    font-size: 1rem;
    margin-top: 0.15rem;
    flex-shrink: 0;
}
.section-description-box__title {
    font-weight: 600;
    margin-bottom: 0.25rem;
    color: var(--primary);
    display: block;
}
.section-description-box__text {
    margin: 0;
    color: var(--text-light);
}
.section-description-box__text em {
    font-style: italic;
}
```

**JS (static/js/accordion.js):** Add a `SECTION_DESCRIPTIONS` constant near the top of the file (after `CATEGORY_DEFINITIONS`), containing the verbatim text from the research doc:

```javascript
const SECTION_DESCRIPTIONS = {
    overview: {
        icon: 'fa-eye',
        title: 'Overview',
        text: 'The Overview provides a high-level summary of the occupation, including the lead statement, definition, and key characteristics from NOC and OaSIS data sources.'
    },
    core_competencies: {
        icon: 'fa-star',
        title: 'Core Competencies',
        text: 'Core competencies represent the fundamental capabilities and personal attributes that are essential for effective performance in this occupation. These include behavioral competencies, work styles, and professional values.'
    },
    activities: {
        icon: 'fa-list-check',
        title: 'Key Activities',
        text: 'Key activities are the main duties and tasks performed in this occupation. They describe what workers in this role typically do on a regular basis.'
    },
    skills: {
        icon: 'fa-lightbulb',
        title: 'Skills (OaSIS Category F)',
        text: '<strong>Developed capabilities</strong> <em>that an individual must have to be effective in a job, role, function, task or duty. Skills are organized into: Foundational Skills (verbal, reading/writing, mathematical), Analytical Skills, Technical Skills, Resource Management Skills, and Interpersonal Skills.</em>'
    },
    abilities: {
        icon: 'fa-brain',
        title: 'Abilities (OaSIS Category A)',
        text: '<strong>Innate and developed aptitudes</strong> <em>that facilitate the acquisition of knowledge and skills to carry out expected work. Abilities include: Cognitive Abilities, Physical Abilities, Psychomotor Abilities, and Sensory Abilities.</em>'
    },
    knowledge: {
        icon: 'fa-book-open',
        title: 'Knowledge (OaSIS Category G)',
        text: '<strong>Organized sets of principles and practices</strong> <em>used for the execution of tasks and activities within a particular domain. Knowledge areas include: Administration &amp; Management, Communication, Education, Health &amp; Wellbeing, Law/Government/Safety, Logistics &amp; Design, Natural Resources, Physical Sciences, Socioeconomic Systems, Technology, and Foundational Knowledge.</em>'
    },
    effort: {
        icon: 'fa-exchange-alt',
        title: 'Effort (OaSIS Work Context J03)',
        text: '<strong>Physical demands</strong> <em>the job requires the worker to perform. This includes body positioning, body exertion, and speaking/seeing requirements.</em>'
    },
    responsibility: {
        icon: 'fa-user',
        title: 'Responsibility (OaSIS Work Context J04)',
        text: '<strong>Interpersonal relations and accountability</strong> <em>required to perform the job. This includes job interactions, communication methods, and interpersonal responsibilities.</em>'
    }
};
```

Add a helper function:

```javascript
const renderSectionDescriptionBox = (tabKey) => {
    const desc = SECTION_DESCRIPTIONS[tabKey];
    if (!desc) return '';
    return `
        <div class="section-description-box">
            <i class="fas ${desc.icon} section-description-box__icon"></i>
            <div>
                <span class="section-description-box__title"><i class="fas ${desc.icon}"></i> ${desc.title}</span>
                <p class="section-description-box__text">${desc.text}</p>
            </div>
        </div>
    `;
};
```

Note: The `text` field for Skills/Abilities/Knowledge/Effort/Responsibility contains raw HTML (bold + italic) so it must NOT be escaped. The Overview, Core Competencies, and Key Activities text is plain and safe.

Then in `renderTabContent()`, prepend description boxes to each panel's innerHTML. For each panel rendering, add the description box BEFORE the existing content:

- `overviewPanel.innerHTML = renderSectionDescriptionBox('overview') + renderOverviewContent(profile);`
- `coreCompPanel.innerHTML = renderSectionDescriptionBox('core_competencies') + renderCoreCompetenciesContent(profile);`
- `activitiesPanel.innerHTML = renderSectionDescriptionBox('activities') + ...existing...;`
- `skillsPanel.innerHTML = renderSectionDescriptionBox('skills') + ...existing...;`
- `abilitiesPanel.innerHTML = renderSectionDescriptionBox('abilities') + ...existing...;`
- `knowledgePanel.innerHTML = renderSectionDescriptionBox('knowledge') + ...existing...;`
- For effort: `effortPanel.innerHTML = renderSectionDescriptionBox('effort') + effortHtml;` (prepend before the entire effort HTML string, AFTER building effortHtml)
- For responsibility: `responsibilityPanel.innerHTML = renderSectionDescriptionBox('responsibility') + respHtml;` (same pattern)

Make sure the source badge still appears at the end (after section description box + content).
  </action>
  <verify>
Run the app, search, click a result. For each of the 8 tabs, verify:
1. Tab button shows the correct FA icon next to its label
2. Blue-grey description box appears at top of tab content
3. Description box has left border accent, info icon, title in bold, and description paragraph
4. Tab content (items, hierarchy, etc.) still renders correctly below the description box
5. No JS console errors
  </verify>
  <done>All 8 tab buttons show FA icons. Every tab panel has a blue-grey section description box at the top with the correct verbatim text from the v5.1 spec. Existing tab content renders unchanged below the description boxes.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign Overview tab with Position Title input and two-column layout</name>
  <files>static/js/accordion.js, static/js/state.js, static/css/main.css</files>
  <action>
**state.js:** Add `positionTitle` key to `defaultState` (at the top level, not inside `selections`):

```javascript
const defaultState = {
    positionTitle: '',        // NEW — editable position title on Overview tab
    selections: {
        // ... existing keys ...
    },
    // ... rest of defaultState ...
};
```

Also reset `positionTitle` to `''` inside `resetSelectionsForProfile` (alongside the selections reset):

```javascript
store.setState({
    positionTitle: '',
    selections: { ... },
    currentProfileCode: nocCode
});
```

**accordion.js — renderOverviewContent():** Rewrite the function. Keep the existing Also Known As panel, NOC Hierarchy table, Feeder Group Mobility, and Other Job Info sections, but restructure the top of the function to add:

1. **Position Title input** at the very top — an editable `<input type="text">` pre-filled with `profile.title`. On change, store the value in `store.getState().positionTitle` via the store. The input should have a label "Position Title" above it.

2. **Two-column layout** for Lead Statement (left) and Definition (right). Lead Statement comes from `profile.reference_attributes.lead_statement`. Definition comes from the async `#profile-description` element (same as current code reads `descEl.textContent`).

3. **Employment Requirements** as a bullet list (from `profile.reference_attributes.employment_requirements` — already rendered in current code via `renderOtherJobInfoContent`; confirm it renders or add it here).

4. **Typical Workplaces** as pill tags (from `profile.reference_attributes.typical_workplaces` — same source as current; confirm pill styling).

New top section of renderOverviewContent:

```javascript
const renderOverviewContent = (profile) => {
    const ref = profile.reference_attributes || {};
    const hierarchy = profile.noc_hierarchy || {};
    const state = store.getState();

    let html = '';

    // Position Title — editable input (BUILD-03)
    const currentTitle = state.positionTitle || profile.title || '';
    html += `
        <div class="overview-position-title">
            <label class="overview-position-title__label" for="position-title-input">Position Title</label>
            <input type="text" id="position-title-input" class="overview-position-title__input"
                   value="${escapeHtml(currentTitle)}"
                   placeholder="Enter position title...">
        </div>
    `;

    // Two-column: Lead Statement + Definition
    const leadStatement = ref.lead_statement || '';
    const descEl = document.getElementById('profile-description');
    const definition = descEl ? descEl.textContent : '';

    html += `
        <div class="overview-two-col">
            <div class="overview-two-col__panel">
                <h4 class="overview-two-col__heading">Lead Statement</h4>
                <p class="overview-two-col__text">${leadStatement ? escapeHtml(leadStatement) : '<em>No lead statement available.</em>'}</p>
            </div>
            <div class="overview-two-col__panel">
                <h4 class="overview-two-col__heading">Definition</h4>
                <p class="overview-two-col__text">${definition ? escapeHtml(definition) : '<em>Loading definition...</em>'}</p>
            </div>
        </div>
    `;

    // ... keep existing Also Known As panel, NOC Hierarchy, Feeder Group, Other Job Info below ...
```

Remove the old `overview-profile-header` div (icon + title + NOC badge at top) — that info is now in the header card. Remove the old `overview-description` div (navy description) — definition is now in the two-column layout.

After the overview panel HTML is set in `renderTabContent()`, attach the Position Title input change handler:

```javascript
if (overviewPanel) {
    overviewPanel.innerHTML = renderSectionDescriptionBox('overview') + renderOverviewContent(profile);

    // Position Title input — store on change
    const titleInput = document.getElementById('position-title-input');
    if (titleInput) {
        titleInput.addEventListener('input', (e) => {
            store.setState({ positionTitle: e.target.value });
        });
    }

    // ... existing "See more" button handler ...
}
```

**CSS (static/css/main.css):** Add styles for the new Overview elements:

```css
/* Overview Position Title Input (BUILD-03) */
.overview-position-title {
    margin-bottom: 1.25rem;
}
.overview-position-title__label {
    display: block;
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--text-light);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 0.35rem;
}
.overview-position-title__input {
    width: 100%;
    padding: 0.6rem 0.75rem;
    font-size: 1.1rem;
    font-weight: 600;
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--text);
    background: #fff;
    transition: border-color 0.2s;
}
.overview-position-title__input:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 3px rgba(38,55,74,0.1);
}

/* Overview Two-Column Layout */
.overview-two-col {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 1.25rem;
}
.overview-two-col__panel {
    background: #f8f9fa;
    border-radius: 6px;
    padding: 1rem;
    border: 1px solid var(--border);
}
.overview-two-col__heading {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.03em;
}
.overview-two-col__text {
    font-size: 0.9rem;
    line-height: 1.55;
    color: var(--text);
    margin: 0;
}

@media (max-width: 768px) {
    .overview-two-col {
        grid-template-columns: 1fr;
    }
}
```
  </action>
  <verify>
Run the app, search, click a result, go to Overview tab. Verify:
1. "Position Title" label with editable text input at top, pre-filled with occupation title
2. Typing in the input stores the value (check via console: `store.getState().positionTitle`)
3. Two-column layout: "Lead Statement" on left, "Definition" on right
4. Also Known As, NOC Hierarchy, Feeder Group sections still render below
5. Load a different profile — Position Title resets to new profile's title
6. No JS console errors
  </verify>
  <done>Overview tab shows editable Position Title input at top that persists in store. Lead Statement and Definition display in two-column grid layout. All existing Overview sections (Also Known As, NOC Hierarchy, Feeder Group, Other Job Info) render unchanged below. Position Title resets on profile change.</done>
</task>

</tasks>

<verification>
1. Load the app at http://127.0.0.1:5000
2. Search for any occupation (e.g., "software")
3. Click a result card to load the Build page
4. Verify dark navy header card: gear icon, title, NOC badge, lead statement, metadata row, close button
5. Click close button — should return to search (step 1)
6. Click same card again, verify header reappears
7. Click through all 8 tabs: Overview, Core Competencies, Key Activities, Skills, Abilities, Knowledge, Effort, Responsibility
8. Each tab button has an icon; each tab panel has the description box at top
9. Overview tab: Position Title input at top (editable), two-column Lead Statement + Definition, existing sections below
10. Existing tab content (items, checkboxes, proficiency ratings) still functional
11. No console errors
</verification>

<success_criteria>
- Dark navy occupation header card with gear icon, NOC code, lead statement, metadata badges, close button
- All 8 tab buttons show FA icons
- All 8 tab panels show section description boxes with correct verbatim text
- Close button returns to search
- Overview tab has editable Position Title input that persists in store
- Overview tab shows two-column Lead Statement + Definition layout
- No regression in existing tab content or selection behavior
</success_criteria>

<output>
After completion, create `.planning/phases/27-build-page-redesign/27-01-SUMMARY.md`
</output>
