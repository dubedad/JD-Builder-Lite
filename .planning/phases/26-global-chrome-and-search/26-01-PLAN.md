---
phase: 26-global-chrome-and-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/index.html
  - static/css/main.css
  - static/js/main.js
  - static/js/sidebar.js
autonomous: true

must_haves:
  truths:
    - "Every page shows a Government of Canada identity header with maple leaf, bilingual org name, and Francais toggle"
    - "Every page shows a dark navy app bar with JD Builder 1.0 badge, truncated session ID, Audit Trail badge, and Reset button"
    - "Every page shows a row of 5 coloured data source pills linking to their source homepages"
    - "Every page shows a 5-step stepper with correct labels: Search, Build, Classify, Generate, Export"
    - "Every page shows a Selections (N) vertical tab on the right edge"
    - "Every page shows a compliance framework paragraph above the footer"
    - "Every page shows a dark footer with JobForge branding left and Canada wordmark right, plus O*NET attribution below"
    - "Francais toggle and View Provenance Graph button render but do nothing when clicked"
  artifacts:
    - path: "templates/index.html"
      provides: "Global chrome HTML structure"
      contains: "gc-identity-header"
    - path: "static/css/main.css"
      provides: "Chrome CSS with design tokens"
      contains: "--app-bar-bg"
    - path: "static/js/main.js"
      provides: "Updated stepper with 5 correct labels"
      contains: "Generate"
    - path: "static/js/sidebar.js"
      provides: "Selections tab trigger wired to existing sidebar"
      contains: "selections-tab"
  key_links:
    - from: "static/js/sidebar.js"
      to: "#sidebar"
      via: "selections-tab click toggles sidebar open/close"
      pattern: "selections-tab.*sidebar"
    - from: "static/js/main.js"
      to: "templates/index.html"
      via: "getOrCreateSessionId populates session ID in app bar"
      pattern: "getOrCreateSessionId|jdb_session_id"
---

<objective>
Build the global application chrome that wraps every page: Government of Canada identity header, dark navy app bar, coloured data source pills, 5-step process stepper, right-edge Selections tab, compliance paragraph, dark footer with O*NET attribution.

Purpose: Establishes the v5.1 branded shell so all subsequent phase work (search, build, classify, generate, export) renders inside a consistent chrome.
Output: Updated index.html with new chrome DOM structure, CSS design tokens and chrome styles in main.css, updated stepper labels in main.js, selections tab trigger in sidebar.js.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-global-chrome-and-search/26-RESEARCH.md
@templates/index.html
@static/css/main.css
@static/js/main.js
@static/js/sidebar.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace chrome HTML in index.html and add design tokens + chrome CSS in main.css</name>
  <files>templates/index.html, static/css/main.css</files>
  <action>
In templates/index.html, replace the existing chrome elements with the v5.1 DOM structure. Keep all content inside `<main class="container">` untouched — only modify the wrapper elements above and below main.

**1. Replace `<header class="gc-header">...</header>` (lines 20-54) with:**

```html
<!-- GoC Identity Header (CHROME-01) -->
<header class="gc-identity-header">
    <div class="container gc-identity-header__inner">
        <div class="gc-identity-header__brand">
            <div class="gc-flag">
                <div class="gc-flag__red"></div>
                <div class="gc-flag__white"><span class="gc-flag__leaf">&#127809;</span></div>
                <div class="gc-flag__red"></div>
            </div>
            <div class="gc-identity-header__titles">
                <span class="gc-identity-header__title-en">Government of Canada</span>
                <span class="gc-identity-header__title-fr">Gouvernement du Canada</span>
            </div>
        </div>
        <div class="gc-identity-header__actions">
            <a href="#" class="gc-identity-header__lang" lang="fr" hreflang="fr" onclick="event.preventDefault();">Fran&ccedil;ais</a>
        </div>
    </div>
</header>
```

Note: Remove the old `.gc-header__search` (Canada.ca search box) and `<nav class="gc-header__nav">` (MENU bar) entirely — they do not exist in v5.1.

**2. Insert new elements between the identity header and the existing stepper nav:**

```html
<!-- App Bar (CHROME-02) -->
<div class="app-bar">
    <div class="container app-bar__inner">
        <span class="app-bar__badge">JD Builder 1.0</span>
        <span class="app-bar__session" id="app-bar-session">Session: ...</span>
        <span class="app-bar__audit" id="app-bar-audit">
            <i class="fas fa-clipboard-list"></i> Audit Trail <span id="audit-count" class="app-bar__audit-count">0</span>
        </span>
        <button class="app-bar__reset" id="app-bar-reset" type="button">
            <i class="fas fa-redo-alt"></i> Reset
        </button>
        <button class="app-bar__provenance" id="provenance-graph-btn" type="button" onclick="event.preventDefault();">
            <i class="fas fa-project-diagram"></i> View Provenance Graph
        </button>
    </div>
</div>

<!-- Data Source Pills (CHROME-03) -->
<div class="data-sources-bar">
    <div class="container data-sources-bar__inner">
        <a href="https://noc.esdc.gc.ca/" class="data-source-pill data-source-pill--noc" target="_blank" rel="noopener noreferrer">2021 NOC</a>
        <a href="https://www.canada.ca/en/department-national-defence/services/caf-careers.html" class="data-source-pill data-source-pill--caf" target="_blank" rel="noopener noreferrer">CAF Careers</a>
        <a href="https://www.canada.ca/en/treasury-board-secretariat/services/collective-agreements/occupational-groups.html" class="data-source-pill data-source-pill--ochro" target="_blank" rel="noopener noreferrer">OCHRO Job Architecture</a>
        <a href="https://www.onetonline.org/" class="data-source-pill data-source-pill--onet" target="_blank" rel="noopener noreferrer">O*NET SOC</a>
        <a href="https://noc.esdc.gc.ca/" class="data-source-pill data-source-pill--oasis" target="_blank" rel="noopener noreferrer">OaSIS</a>
    </div>
</div>
```

**3. Update the existing stepper `<nav class="jd-stepper">` (lines 57-92):**

Replace the 5 `<li>` items with the correct v5.1 labels. The current stepper has: Search, Select Profile, Build JD, Export, Classify. The v5.1 labels are: 1=Search, 2=Build, 3=Classify, 4=Generate, 5=Export.

```html
<li class="jd-stepper__step jd-stepper__step--active" data-step="1">
    <button class="jd-stepper__btn" data-step="1">
        <span class="jd-stepper__number">1</span>
        <span class="jd-stepper__label">Search</span>
    </button>
</li>
<li class="jd-stepper__step" data-step="2">
    <button class="jd-stepper__btn" data-step="2" disabled>
        <span class="jd-stepper__number">2</span>
        <span class="jd-stepper__label">Build</span>
    </button>
</li>
<li class="jd-stepper__step" data-step="3">
    <button class="jd-stepper__btn" data-step="3" disabled>
        <span class="jd-stepper__number">3</span>
        <span class="jd-stepper__label">Classify</span>
    </button>
</li>
<li class="jd-stepper__step" data-step="4">
    <button class="jd-stepper__btn" data-step="4" disabled>
        <span class="jd-stepper__number">4</span>
        <span class="jd-stepper__label">Generate</span>
    </button>
</li>
<li class="jd-stepper__step" data-step="5">
    <button class="jd-stepper__btn" data-step="5" disabled>
        <span class="jd-stepper__number">5</span>
        <span class="jd-stepper__label">Export</span>
    </button>
</li>
```

**4. Add Selections tab button before `</body>`** (CHROME-05):

```html
<!-- Selections Tab (CHROME-05) -->
<button class="selections-tab" id="selections-tab-btn" type="button">
    Selections (<span id="selections-tab-count">0</span>)
</button>
```

**5. Insert compliance paragraph before the footer** (CHROME-06):

```html
<!-- Compliance Framework (CHROME-06) -->
<div class="compliance-bar">
    <div class="container">
        <p class="compliance-bar__text">
            Job Description content is traceable to its sources, data stewards, and publishing dates for full medallion provenance that complies with the Directive on Automated Decision Making requirements.
        </p>
    </div>
</div>
```

**6. Replace `<footer class="gc-footer">...</footer>` (lines 526-547) with:**

```html
<!-- App Footer (CHROME-07) -->
<footer class="app-footer">
    <div class="container app-footer__inner">
        <span class="app-footer__brand">JobForge &mdash; JD Builder 1.0 | Full Audit Trail Provenance</span>
        <div class="app-footer__wordmark">
            <span class="gc-wordmark__text">Canada</span>
            <span class="gc-wordmark__flag">&#127809;</span>
        </div>
    </div>
</footer>
<div class="onet-attribution">
    <div class="container">
        <p class="onet-attribution__text">O*NET&trade; is a trademark of the U.S. Department of Labor, Employment and Training Administration (USDOL/ETA).</p>
    </div>
</div>
```

**7. In static/css/main.css, add design tokens to the existing `:root` block:**

```css
/* v5.1 Design Tokens */
--app-bar-bg: #26374a;
--app-bar-badge-bg: #4a6fa5;
--pill-noc-bg: #555555;
--pill-caf-bg: #28a745;
--pill-ochro-bg: #fd7e14;
--pill-onet-bg: #17a2b8;
--pill-oasis-bg: #6f42c1;
--badge-green-bg: #d4edda; --badge-green-text: #155724;
--badge-blue-bg: #cce5ff;  --badge-blue-text: #004085;
--badge-grey-bg: #e2e3e5;  --badge-grey-text: #383d41;
--stepper-active-bg: #26374a;
--stepper-inactive-bg: #e0e0e0;
--stepper-completed-bg: #28a745;
--footer-bg: #26374a;
```

**8. Add chrome CSS classes in main.css** (after existing styles). Key classes to implement:

- `.gc-identity-header` — white bg, border-bottom, flex row with brand left and lang toggle right
- `.app-bar` — background: var(--app-bar-bg), white text, flex row, padding 0.5rem 0
- `.app-bar__badge` — background: var(--app-bar-badge-bg), border-radius 4px, padding 0.25rem 0.75rem, font-weight bold
- `.app-bar__session` — font-size 0.85rem, opacity 0.8
- `.app-bar__audit` — font-size 0.85rem
- `.app-bar__audit-count` — background: white, color: var(--app-bar-bg), border-radius 50%, display inline-block, width 1.5em, text-align center
- `.app-bar__reset` — border: 1px solid white, background transparent, color white, border-radius 4px
- `.app-bar__provenance` — same style as reset button
- `.data-sources-bar` — background: #f5f5f5, padding 0.5rem 0, border-bottom
- `.data-source-pill` — display inline-block, border-radius 12px, padding 0.25rem 0.75rem, color white, font-size 0.8rem, text-decoration none, margin-right 0.5rem
- `.data-source-pill--noc` bg: var(--pill-noc-bg); `--caf` bg: var(--pill-caf-bg); `--ochro` bg: var(--pill-ochro-bg); `--onet` bg: var(--pill-onet-bg); `--oasis` bg: var(--pill-oasis-bg)
- `.selections-tab` — position fixed, right 0, top 50%, transform translateY(-50%), writing-mode vertical-rl, text-orientation mixed, background var(--app-bar-bg), color white, padding 1rem 0.5rem, cursor pointer, z-index 200, border-radius 4px 0 0 4px, border none, font-size 0.9rem
- `.compliance-bar` — background #f8f9fa, padding 1rem 0, border-top 1px solid #dee2e6, margin-top 2rem
- `.compliance-bar__text` — font-size 0.85rem, color #555, text-align center
- `.app-footer` — background var(--footer-bg), color white, padding 1rem 0
- `.app-footer__inner` — display flex, justify-content space-between, align-items center
- `.onet-attribution` — background #f5f5f5, padding 0.5rem 0, text-align center, font-size 0.75rem, color #777

**9. Update existing stepper CSS** to use the new design tokens:

- `.jd-stepper__step--active .jd-stepper__number` — background: var(--stepper-active-bg)
- `.jd-stepper__step--completed .jd-stepper__number` — background: var(--stepper-completed-bg)
- Non-active steps: background: var(--stepper-inactive-bg)

**10. Remove or override old chrome CSS** — any `.gc-header__search`, `.gc-header__nav`, `.gc-menu-btn` rules should be removed or overridden since those DOM elements no longer exist.

IMPORTANT: Do NOT remove or modify anything inside `<main class="container">...</main>`. Only modify the chrome wrapper above and below it. Do NOT modify the `<aside id="sidebar">` element itself — only add the new selections-tab button.
  </action>
  <verify>
Run `PYTHONPATH=. python3 src/app.py` and visit http://127.0.0.1:5000.
Visually confirm:
- GoC identity header with maple leaf and "Francais" toggle (clicking does nothing)
- Dark navy app bar with "JD Builder 1.0" badge, session ID, Audit Trail 0, Reset button, and "View Provenance Graph" button (clicking does nothing)
- Row of 5 coloured pills (grey NOC, green CAF, orange OCHRO, blue O*NET, purple OaSIS) — each links to correct external URL
- 5-step stepper reads: Search, Build, Classify, Generate, Export
- Right-edge vertical "Selections (0)" tab visible
- Compliance paragraph above footer
- Dark footer with "JobForge — JD Builder 1.0 | Full Audit Trail Provenance" left and Canada wordmark right
- O*NET attribution below footer
  </verify>
  <done>All 8 chrome elements (CHROME-01 through CHROME-08) render correctly on page load. No JS errors in console. Existing search/build/classify functionality is not broken.</done>
</task>

<task type="auto">
  <name>Task 2: Wire JS for session ID, audit count, selections count, reset button, and stepper remapping</name>
  <files>static/js/main.js, static/js/sidebar.js</files>
  <action>
**1. In static/js/main.js, add session ID logic** at the top of the DOMContentLoaded handler (before `initSidebar()`):

```javascript
// Session ID (CHROME-02)
function getOrCreateSessionId() {
    let id = localStorage.getItem('jdb_session_id');
    if (!id) {
        id = crypto.randomUUID().replace(/-/g, '').substring(0, 12);
        localStorage.setItem('jdb_session_id', id);
    }
    return id;
}
const sessionId = getOrCreateSessionId();
const sessionEl = document.getElementById('app-bar-session');
if (sessionEl) sessionEl.textContent = 'Session: ' + sessionId.substring(0, 8) + '...';
```

**2. Add Reset button handler** (clears localStorage, sessionStorage, reloads):

```javascript
const resetBtn = document.getElementById('app-bar-reset');
if (resetBtn) {
    resetBtn.addEventListener('click', () => {
        if (confirm('Reset session? This will clear all selections and cached data.')) {
            localStorage.removeItem('jdb_session_id');
            localStorage.removeItem('classification_cache');
            store.reset();
            window.location.reload();
        }
    });
}
```

**3. Update the `navigateToStep()` switch statement** (around line 787) to remap step numbers to match the new v5.1 labels:

Current mapping:
- case 1: Search (keep)
- case 2: Select Profile (was "show search results") -> now "Build" — show profile/tabs
- case 3: Build JD (was "show profile") -> now "Classify" — show classify section
- case 4: Export (was "open sidebar") -> now "Generate" — show overview section
- case 5: Classify (was "run classification") -> now "Export" — open sidebar

Update the switch cases:
- case 1: Search — same as current case 1 (show welcome/search-results)
- case 2: Build — merge logic from current cases 2+3 (show profile + tabs if profile loaded, else show search results)
- case 3: Classify — use logic from current case 5 (show classify section, trigger classification)
- case 4: Generate — show overview-section, hide others (new behavior)
- case 5: Export — use logic from current case 4 (open sidebar for export)

Also update `canAccessStep()` accordingly:
- case 1: always true
- case 2: need a profile loaded (`window.currentProfile !== null`)
- case 3: need selections (same logic as old case 5)
- case 4: need profile loaded
- case 5: need profile loaded

Update any event listeners that reference old step numbers:
- `search-complete` event should still update to step 1 (search results shown)
- `profile-loaded` event should update to step 2 (build)

**4. Update audit trail count** — subscribe to store changes and update `#audit-count`:

```javascript
// After store is available
store.subscribe((state) => {
    const auditEl = document.getElementById('audit-count');
    if (auditEl) {
        const total = Object.values(state.selections)
            .reduce((sum, arr) => sum + arr.length, 0);
        auditEl.textContent = total;
    }
});
```

**5. In static/js/sidebar.js**, repurpose the sidebar trigger:

- Hide the old `#sidebar-toggle` button (add `display: none` inline or via CSS)
- Add an event listener on `#selections-tab-btn` that toggles the sidebar open/close (same toggle logic as the old button):

```javascript
// Hide old toggle
const oldToggle = document.getElementById('sidebar-toggle');
if (oldToggle) oldToggle.style.display = 'none';

// Wire new selections tab
const selectionsTab = document.getElementById('selections-tab-btn');
if (selectionsTab) {
    selectionsTab.addEventListener('click', () => {
        sidebar.classList.toggle('open');
        sidebar.classList.toggle('collapsed');
        document.body.classList.toggle('sidebar-open');
    });
}
```

- Update `updateSidebar()` to also update the selections tab count:

```javascript
// At end of updateSidebar function:
const tabCount = document.getElementById('selections-tab-count');
if (tabCount) {
    tabCount.textContent = totalSelections;
}
```

IMPORTANT: Do NOT rewrite the sidebar content rendering logic — keep the existing `updateSidebar()` HTML generation. Only add the count update and new trigger wiring.
  </action>
  <verify>
Run the app and test:
1. Session ID appears truncated in app bar (e.g., "Session: a1b2c3d4...")
2. Audit Trail count starts at 0, increments when statements are selected on Build tab
3. Reset button prompts confirmation, clears data, reloads page with new session ID
4. Stepper labels match v5.1: Search, Build, Classify, Generate, Export
5. Clicking "Selections (0)" tab on right edge toggles sidebar open/close
6. Selections count updates when items are selected
7. Navigating between steps works: Search(1) -> search -> click result -> Build(2) shows profile
8. "View Provenance Graph" button renders but does nothing
9. No JS console errors
  </verify>
  <done>App bar shows live session ID and audit count. Reset clears state. Stepper navigates correctly with v5.1 step labels (Search/Build/Classify/Generate/Export). Selections tab toggles sidebar and shows live count. All CHROME requirements (01-08) are functionally wired.</done>
</task>

</tasks>

<verification>
1. `PYTHONPATH=. python3 src/app.py` — app starts without errors
2. Visit http://127.0.0.1:5000 — all 8 chrome elements visible
3. Click each data source pill — opens correct external URL in new tab
4. Click Francais toggle — nothing happens (cosmetic only, CHROME-08)
5. Click View Provenance Graph — nothing happens (cosmetic only, CHROME-08)
6. Perform a search, click a result, verify stepper progresses through steps correctly
7. Select some statements, verify Audit Trail count and Selections tab count update
8. Click Selections tab — sidebar opens showing selections
9. Click Reset — session clears, page reloads
</verification>

<success_criteria>
- All 8 CHROME requirements (CHROME-01 through CHROME-08) render correctly
- Stepper labels match v5.1 spec: Search, Build, Classify, Generate, Export
- Step navigation works correctly with remapped step numbers
- Session ID persists across page reloads until Reset
- Audit Trail count reflects total selections in real time
- Selections tab opens/closes the sidebar
- No regressions in search/build/classify functionality
</success_criteria>

<output>
After completion, create `.planning/phases/26-global-chrome-and-search/26-01-SUMMARY.md`
</output>
