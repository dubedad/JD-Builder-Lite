---
phase: 26-global-chrome-and-search
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - templates/index.html
  - static/css/results-cards.css
  - static/css/filters.css
  - static/js/main.js
  - src/models/noc.py
  - src/services/search_parquet_reader.py
autonomous: true

must_haves:
  truths:
    - "Search page displays 'Find your Job' heading, subtitle, match quality legend, and centered search input"
    - "Empty state shows magnifying glass icon with 'Start typing to search' text when no query entered"
    - "Search results show a left filter panel with 6 accordion sections"
    - "Result cards display occupation icon, bold title, match badge pills with score% and source, Also known as line, and description"
    - "Results header shows count, + New Search button, and Published/Data Steward metadata"
  artifacts:
    - path: "templates/index.html"
      provides: "Redesigned search section HTML"
      contains: "Find your Job"
    - path: "static/css/results-cards.css"
      provides: "v5.1 result card styles with match badge pills"
      contains: "match-badge-pill"
    - path: "static/css/filters.css"
      provides: "6-accordion filter panel styles"
      contains: "filter-accordion"
    - path: "static/js/main.js"
      provides: "Updated renderCardView with match badges and also-known-as"
      contains: "also_known_as"
    - path: "src/models/noc.py"
      provides: "source_label field on EnrichedSearchResult"
      contains: "source_label"
    - path: "src/services/search_parquet_reader.py"
      provides: "example_titles populated from titles_df"
      contains: "example_titles"
  key_links:
    - from: "src/services/search_parquet_reader.py"
      to: "src/models/noc.py"
      via: "EnrichedSearchResult constructor sets source_label and example_titles"
      pattern: "source_label.*O.NET|example_titles"
    - from: "static/js/main.js"
      to: "templates/index.html"
      via: "renderCardView generates card HTML with match-badge-pill and also-known-as"
      pattern: "match-badge-pill|also.known.as"
    - from: "static/js/main.js"
      to: "#filter-panel"
      via: "filterModule populates 6 accordion sections from search results"
      pattern: "filter-accordion"
---

<objective>
Redesign the search page with "Find your Job" heading, empty-state magnifying glass, v5.1 result cards (match badge pills, "Also known as:" line, description), 6-accordion filter panel, and results header with count/metadata.

Purpose: Transforms the search experience from the v5.0 OaSIS-style layout to the v5.1 JobForge design with match quality indicators and rich filtering.
Output: Redesigned search HTML in index.html, new card and filter CSS, updated renderCardView in main.js, backend changes for source_label and example_titles.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-global-chrome-and-search/26-RESEARCH.md
@.planning/phases/26-global-chrome-and-search/26-01-SUMMARY.md
@templates/index.html
@static/css/results-cards.css
@static/css/filters.css
@static/js/main.js
@src/models/noc.py
@src/services/search_parquet_reader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — add source_label and populate example_titles</name>
  <files>src/models/noc.py, src/services/search_parquet_reader.py</files>
  <action>
**1. In src/models/noc.py**, add `source_label` field to `EnrichedSearchResult`:

Add after the `publication_date` field (around line 59):

```python
# Match source label for badge display (v5.1 SRCH-04)
source_label: Optional[str] = None  # e.g., "O*NET SOC", "2021 NOC"
```

**2. In src/services/search_parquet_reader.py**, update `_build_result()` to:

a) Set `source_label` to `"O*NET SOC"` by default (all parquet results come from O*NET-aligned data):

```python
source_label="O*NET SOC",
```

b) Populate `example_titles` from `titles_df` lookup. After building `lead_statement`, add:

```python
# Populate example_titles from titles_df (SRCH-04 "Also known as:")
example_titles_str = None
if self._titles_df is not None:
    title_matches = self._titles_df[
        self._titles_df["unit_group_id"] == noc_code
    ]
    if not title_matches.empty:
        titles_list = title_matches["Job title text"].dropna().tolist()
        if titles_list:
            example_titles_str = "; ".join(titles_list[:5])  # Limit to 5
```

Then pass `example_titles=example_titles_str` to the `EnrichedSearchResult` constructor.

c) Update the `_build_result` method signature to accept `titles_df` or use `self._titles_df` directly (it's already an instance variable).

d) Derive `match_tier` from relevance_score for the match badge text. Instead of changing the model, this will be done on the frontend in Task 2 — no additional backend changes needed here. The existing `relevance_score` and `match_reason` fields are sufficient.

IMPORTANT: The `_build_result` method currently does NOT receive `titles_df` — it uses `self._titles_df` which is available as an instance attribute. Use `self._titles_df` directly inside `_build_result`.
  </action>
  <verify>
Run `PYTHONPATH=. python3 -c "from src.services.search_parquet_reader import search_parquet_reader; results = search_parquet_reader.search('engineer'); print([(r.title, r.source_label, r.example_titles) for r in (results or [])[:3]])"` — should show source_label="O*NET SOC" and example_titles with semicolon-separated titles for each result.
  </verify>
  <done>EnrichedSearchResult includes source_label (defaulting to "O*NET SOC") and example_titles is populated from titles_df lookup. API response JSON now includes both fields.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — redesign search HTML, result cards, filter panel, and render logic</name>
  <files>templates/index.html, static/css/results-cards.css, static/css/filters.css, static/js/main.js</files>
  <action>
**1. In templates/index.html, replace `#welcome-section`** (lines 96-138) with the v5.1 search landing:

```html
<section id="welcome-section" class="welcome-section">
    <div class="search-landing">
        <h1 class="search-landing__title">Find your Job</h1>
        <p class="search-landing__subtitle">Search across all job taxonomies to find the occupation that best matches your position</p>

        <!-- Match Quality Legend (SRCH-01) -->
        <div class="match-quality-legend">
            <span class="match-quality-legend__item match-quality-legend__item--green">
                <span class="match-quality-legend__pct">95-100%</span> Title
            </span>
            <span class="match-quality-legend__item match-quality-legend__item--blue">
                <span class="match-quality-legend__pct">85%</span> Description
            </span>
            <span class="match-quality-legend__item match-quality-legend__item--grey">
                <span class="match-quality-legend__pct">75%</span> Related
            </span>
        </div>

        <!-- Search Input (SRCH-01) -->
        <div class="search-container search-container--centered">
            <div class="search-input-row">
                <input
                    type="search"
                    id="search-input"
                    placeholder="Search job titles, NOC codes, or keywords..."
                    aria-label="Search job titles"
                >
                <button id="search-button" type="button"><i class="fas fa-search"></i> Search</button>
            </div>
            <div class="search-type-toggle" role="tablist">
                <button role="tab" class="pill-btn active" data-search-type="Keyword" aria-selected="true">Keyword</button>
                <button role="tab" class="pill-btn" data-search-type="Code" aria-selected="false">Code</button>
            </div>
        </div>

        <p class="search-landing__source">
            Source: <a href="https://noc.esdc.gc.ca/" target="_blank" rel="noopener noreferrer">JobForge WiQ</a> | All job taxonomies
        </p>

        <!-- Empty State (SRCH-02) -->
        <div id="search-empty-state" class="search-empty-state">
            <i class="fas fa-search search-empty-state__icon"></i>
            <p class="search-empty-state__text">Start typing to search across all job taxonomies</p>
            <p class="search-empty-state__compliance">
                Job Description content is traceable to its sources, data stewards, and publishing dates for full medallion provenance.
            </p>
        </div>
    </div>
</section>
```

Note: Remove the old "Welcome to JobForge" layout, the logo image reference, the "Job Family" disabled pill button, and the old authoritative sources paragraphs. The search-type-toggle moves below the search input. Keep element IDs `search-input` and `search-button` so existing JS wiring works.

**2. In templates/index.html, update `#search-results`** (lines 162-265):

Replace the sort controls and results header:

```html
<section id="search-results" class="search-results hidden">
    <!-- Results Header (SRCH-05) -->
    <div class="results-header">
        <div class="results-header__left">
            <h2 id="results-count" class="results-header__count">0 results</h2>
            <span class="results-header__filter-status" id="filter-status">(filtered)</span>
        </div>
        <div class="results-header__right">
            <button id="new-search-btn" class="btn btn--secondary btn--sm" type="button">
                <i class="fas fa-plus"></i> New Search
            </button>
            <span class="results-header__meta">
                <span>Published: 2024-11-15</span> | <span>Data Steward: ESDC</span>
            </span>
        </div>
    </div>
    <!-- ...keep filter-toggle button as-is... -->
```

Replace the filter panel `<aside id="filter-panel">` with 6 v5.1 accordion sections:

```html
<aside id="filter-panel" class="filter-panel">
    <h3>Filters</h3>

    <details class="filter-accordion" id="filter-ochro" open>
        <summary>OCHRO Job Architecture</summary>
        <div class="filter-accordion__body">
            <p class="filter-empty">No results</p>
        </div>
    </details>

    <details class="filter-accordion" id="filter-caf">
        <summary>CAF Careers</summary>
        <div class="filter-accordion__body">
            <p class="filter-empty">No results</p>
        </div>
    </details>

    <details class="filter-accordion" id="filter-noc-broad" open>
        <summary>2021 NOC Broad &rarr; Major</summary>
        <div id="filter-noc-broad-options" class="filter-accordion__body filter-options">
            <p class="filter-empty">Search to see filter options</p>
        </div>
    </details>

    <details class="filter-accordion" id="filter-noc-teer" open>
        <summary>2021 NOC TEER</summary>
        <div id="filter-noc-teer-options" class="filter-accordion__body filter-options">
            <p class="filter-empty">Search to see filter options</p>
        </div>
    </details>

    <details class="filter-accordion" id="filter-occ-groups">
        <summary>Occupational Groups</summary>
        <div class="filter-accordion__body">
            <p class="filter-empty">No results</p>
        </div>
    </details>

    <details class="filter-accordion" id="filter-onet-soc">
        <summary>O*NET SOC</summary>
        <div class="filter-accordion__body">
            <p class="filter-empty">No results</p>
        </div>
    </details>

    <button id="filter-clear" class="filter-clear" type="button" disabled>
        <i class="fas fa-times"></i> Clear All Filters
    </button>

    <!-- Match Confidence Legend -->
    <div class="scoring-legend">
        <h4 class="scoring-legend__title">Match Confidence</h4>
        <ul class="scoring-legend__list">
            <li><span class="scoring-legend__pct scoring-legend__pct--green">95-100%</span> Title match</li>
            <li><span class="scoring-legend__pct scoring-legend__pct--blue">80-94%</span> Description match</li>
            <li><span class="scoring-legend__pct scoring-legend__pct--grey">50-79%</span> Related match</li>
        </ul>
    </div>
</aside>
```

Keep the `.results-main` and `#results-list` div as-is (populated by JS).

Remove the old `.results-footer` div (compliance text is now in the global chrome compliance-bar).

**3. In static/css/results-cards.css**, replace the card styles with v5.1 design:

Replace the existing `.oasis-card` styles with new result card classes:

```css
/* v5.1 Result Cards (SRCH-04) */
.result-card {
    background: white;
    border: 1px solid #dee2e6;
    border-radius: 8px;
    padding: 1.25rem;
    margin-bottom: 1rem;
    cursor: pointer;
    transition: box-shadow 0.2s, border-color 0.2s;
}
.result-card:hover {
    box-shadow: 0 2px 8px rgba(0,0,0,0.12);
    border-color: #adb5bd;
}
.result-card__header {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
}
.result-card__icon {
    font-size: 1.5rem;
    color: var(--app-bar-bg, #26374a);
    flex-shrink: 0;
    width: 2rem;
    text-align: center;
}
.result-card__title {
    font-size: 1.1rem;
    font-weight: 700;
    color: #1a1a1a;
    margin: 0;
}
.result-card__badges {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
}
.match-badge-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.2rem 0.6rem;
    border-radius: 12px;
    font-size: 0.8rem;
    font-weight: 600;
}
.match-badge-pill--green {
    background: var(--badge-green-bg, #d4edda);
    color: var(--badge-green-text, #155724);
}
.match-badge-pill--blue {
    background: var(--badge-blue-bg, #cce5ff);
    color: var(--badge-blue-text, #004085);
}
.match-badge-pill--grey {
    background: var(--badge-grey-bg, #e2e3e5);
    color: var(--badge-grey-text, #383d41);
}
.result-card__also-known-as {
    font-size: 0.9rem;
    color: #555;
    margin-bottom: 0.5rem;
}
.result-card__also-known-as strong {
    color: #333;
}
.result-card__also-known-as .highlight {
    background: #fff3cd;
    padding: 0 2px;
    border-radius: 2px;
}
.result-card__description {
    font-size: 0.9rem;
    color: #666;
    line-height: 1.5;
    margin: 0;
}
```

Keep existing grid-view styles as they are (grid view is a secondary view mode).

**4. In static/css/filters.css**, add/update accordion styles:

```css
/* v5.1 Filter Accordion (SRCH-03) */
.filter-accordion {
    border: 1px solid #dee2e6;
    border-radius: 4px;
    margin-bottom: 0.5rem;
}
.filter-accordion > summary {
    padding: 0.75rem 1rem;
    cursor: pointer;
    font-weight: 600;
    font-size: 0.9rem;
    background: #f8f9fa;
    list-style: none;
}
.filter-accordion > summary::-webkit-details-marker {
    display: none;
}
.filter-accordion > summary::before {
    content: '\f078'; /* fa-chevron-down */
    font-family: 'Font Awesome 6 Free';
    font-weight: 900;
    font-size: 0.7rem;
    margin-right: 0.5rem;
    transition: transform 0.2s;
}
.filter-accordion[open] > summary::before {
    transform: rotate(180deg);
}
.filter-accordion__body {
    padding: 0.75rem 1rem;
}
.filter-empty {
    font-size: 0.85rem;
    color: #999;
    font-style: italic;
}
```

**5. Add search landing CSS** to results-cards.css or main.css (your choice — keep with search-related styles):

```css
/* Search Landing (SRCH-01, SRCH-02) */
.search-landing {
    text-align: center;
    padding: 3rem 1rem;
    max-width: 700px;
    margin: 0 auto;
}
.search-landing__title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--app-bar-bg, #26374a);
    margin-bottom: 0.5rem;
}
.search-landing__subtitle {
    font-size: 1.1rem;
    color: #555;
    margin-bottom: 1.5rem;
}
.match-quality-legend {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
}
.match-quality-legend__item {
    font-size: 0.85rem;
    display: flex;
    align-items: center;
    gap: 0.25rem;
}
.match-quality-legend__pct {
    font-weight: 700;
}
.match-quality-legend__item--green .match-quality-legend__pct { color: #155724; }
.match-quality-legend__item--blue .match-quality-legend__pct { color: #004085; }
.match-quality-legend__item--grey .match-quality-legend__pct { color: #383d41; }
.search-container--centered {
    max-width: 500px;
    margin: 0 auto 1rem;
}
.search-landing__source {
    font-size: 0.85rem;
    color: #777;
    margin-bottom: 2rem;
}
.search-empty-state {
    padding: 2rem;
}
.search-empty-state__icon {
    font-size: 4rem;
    color: #dee2e6;
    margin-bottom: 1rem;
}
.search-empty-state__text {
    font-size: 1.1rem;
    color: #888;
    margin-bottom: 0.5rem;
}
.search-empty-state__compliance {
    font-size: 0.8rem;
    color: #aaa;
}
/* Results Header (SRCH-05) */
.results-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 1rem;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid #dee2e6;
}
.results-header__count {
    font-size: 1.2rem;
    font-weight: 600;
    margin: 0;
}
.results-header__left {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}
.results-header__filter-status {
    font-size: 0.85rem;
    color: #777;
}
.results-header__right {
    display: flex;
    align-items: center;
    gap: 1rem;
}
.results-header__meta {
    font-size: 0.8rem;
    color: #777;
}
```

**6. In static/js/main.js, update `renderCardView()`** function to produce the new v5.1 card HTML:

Replace the existing `renderCardView()` function body. For each result:

```javascript
function renderCardView(results) {
    resultsList.className = 'results-cards-container';

    results.forEach(function(result) {
        const card = document.createElement('div');
        card.className = 'result-card';
        card.setAttribute('data-code', result.noc_code);
        card.setAttribute('role', 'listitem');
        card.setAttribute('tabindex', '0');

        // Determine match tier for badge color
        const score = result.relevance_score || 0;
        let badgeClass, badgeLabel;
        if (score >= 95) {
            badgeClass = 'match-badge-pill--green';
            badgeLabel = 'Title match';
        } else if (score >= 80) {
            badgeClass = 'match-badge-pill--blue';
            badgeLabel = 'Description match';
        } else {
            badgeClass = 'match-badge-pill--grey';
            badgeLabel = 'Related match';
        }

        const sourceLabel = result.source_label || 'O*NET SOC';

        // Build "Also known as:" with keyword highlighting
        let alsoKnownAsHtml = '';
        if (result.example_titles) {
            let titlesText = escapeHtml(result.example_titles);
            // Highlight search query keywords
            if (searchInput.value.trim()) {
                const queryWords = searchInput.value.trim().toLowerCase().split(/\s+/);
                queryWords.forEach(word => {
                    if (word.length >= 3) {
                        const regex = new RegExp('(' + word.replace(/[.*+?^${}()|[\]\\]/g, '\\$&') + ')', 'gi');
                        titlesText = titlesText.replace(regex, '<span class="highlight">$1</span>');
                    }
                });
            }
            alsoKnownAsHtml = `<p class="result-card__also-known-as"><strong>Also known as:</strong> ${titlesText}</p>`;
        }

        card.innerHTML = `
            <div class="result-card__header">
                <i class="fas ${getNocCategoryIcon(result.noc_code)} result-card__icon" aria-hidden="true"></i>
                <h3 class="result-card__title">${escapeHtml(result.title)}</h3>
            </div>
            <div class="result-card__badges">
                <span class="match-badge-pill ${badgeClass}">${score}% ${badgeLabel}</span>
                <span class="match-badge-pill match-badge-pill--grey">${escapeHtml(sourceLabel)}</span>
            </div>
            ${alsoKnownAsHtml}
            ${result.lead_statement ? `<p class="result-card__description">${escapeHtml(result.lead_statement)}</p>` : ''}
        `;

        resultsList.appendChild(card);
    });
}
```

**7. Add "+ New Search" button handler** and empty-state toggle in main.js:

```javascript
// + New Search button
const newSearchBtn = document.getElementById('new-search-btn');
if (newSearchBtn) {
    newSearchBtn.addEventListener('click', () => {
        searchInput.value = '';
        searchResults.classList.add('hidden');
        document.getElementById('welcome-section').classList.remove('hidden');
        document.getElementById('search-empty-state')?.classList.remove('hidden');
        lastResults = [];
        window.jdStepper.goToStep(1);
    });
}
```

In the existing `handleSearch()` function, hide the empty state when search starts:

```javascript
document.getElementById('search-empty-state')?.classList.add('hidden');
```

**8. Update `renderSearchResults()`** to update the results count in the new header format:

```javascript
// Update to: "[N] results (filtered)" or "[N] results"
const countEl = document.getElementById('results-count');
if (countEl) {
    countEl.textContent = `${results.length} result${results.length !== 1 ? 's' : ''}`;
}
```

**9. Update filter panel population** in the existing `filterModule.init()` or related code:

The existing filter uses `#filter-minor-group-options` for NOC categories. Update the filter module to populate:
- `#filter-noc-broad-options` — from `broad_category` and `broad_category_name` fields (existing data)
- `#filter-noc-teer-options` — derive TEER from 2nd digit of NOC code (e.g., 21234 -> TEER 1)
- Other 4 sections (OCHRO, CAF, Occupational Groups, O*NET SOC) — always show "No results" (research confirms no data available)

If the existing `filters.js` uses the old element ID `#filter-minor-group-options`, update it to use `#filter-noc-broad-options` instead. Check filters.js for the exact ID references and update accordingly.

IMPORTANT: Keep the old sort controls `#sort-select` and `#view-toggle` functional but move them inside the results header or remove them if they conflict with the new layout. If removing sort, keep the `sortResults()` function accessible for future use.
  </action>
  <verify>
1. Run `PYTHONPATH=. python3 src/app.py` and visit http://127.0.0.1:5000
2. See "Find your Job" heading with subtitle, match quality legend, centered search input
3. See magnifying glass empty state below search input
4. Type "engineer" and search — empty state disappears, results appear
5. Result cards show: occupation icon, bold title, match badge pills (green/blue/grey with score %), source badge "O*NET SOC", "Also known as:" line with highlighted keywords, and lead statement description
6. Left filter panel shows 6 accordion sections — NOC Broad and NOC TEER populated, others show "No results"
7. Results header shows "[N] results", "+ New Search" button, and "Published: 2024-11-15 | Data Steward: ESDC"
8. Click "+ New Search" — returns to empty search landing
9. Click a result card — loads profile (existing functionality preserved)
  </verify>
  <done>Search page matches v5.1 design: "Find your Job" heading, empty state with magnifying glass, match badge pills on result cards, "Also known as:" with keyword highlighting, 6-accordion filter panel, results header with count and metadata. All 5 SRCH requirements (SRCH-01 through SRCH-05) are met.</done>
</task>

</tasks>

<verification>
1. `PYTHONPATH=. python3 src/app.py` — app starts without errors
2. Visit http://127.0.0.1:5000 — "Find your Job" heading visible with match quality legend
3. Empty state magnifying glass visible before any search
4. Search "nurse" — results appear with green/blue/grey match badge pills
5. Each card shows: icon, bold title, score badges, "Also known as:" with highlights, description
6. Filter panel left side: 6 accordion sections, NOC Broad and TEER populated, 4 others "No results"
7. Results header: "[N] results", "+ New Search" button, ESDC metadata
8. Click "+ New Search" — returns to clean search landing
9. Click result card — profile loads correctly (existing flow works)
10. Full flow: Search -> click result -> Build tab -> select statements -> Classify -> works end-to-end
</verification>

<success_criteria>
- SRCH-01: "Find your Job" heading, subtitle, match quality legend, centered search input, source link all visible
- SRCH-02: Magnifying glass empty state with "Start typing" text visible when no query
- SRCH-03: Left filter panel with 6 accordion sections (2 functional, 4 placeholder), Clear All Filters button, Match Confidence legend
- SRCH-04: Result cards show occupation icon, bold title, match badge pills (score% + tier), source badge, "Also known as:" with keyword highlighting, description paragraph
- SRCH-05: Results header with "[N] results (filtered)", "+ New Search" button, "Published: 2024-11-15 | Data Steward: ESDC"
- No regressions in profile loading, statement selection, classification, or export
</success_criteria>

<output>
After completion, create `.planning/phases/26-global-chrome-and-search/26-02-SUMMARY.md`
</output>
