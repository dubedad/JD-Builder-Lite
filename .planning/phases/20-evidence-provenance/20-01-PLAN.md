---
phase: 20-evidence-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/js/classify.js
autonomous: true

must_haves:
  truths:
    - "User can see expandable provenance tree from recommendation to TBS source"
    - "Provenance tree shows three-level chain: Recommendation -> Definition -> TBS Page"
    - "User can expand/collapse provenance detail with ARIA support"
  artifacts:
    - path: "static/js/classify.js"
      provides: "Expandable provenance tree rendering"
      contains: "provenance-tree"
    - path: "static/css/evidence.css"
      provides: "Provenance tree styles (already exists)"
      contains: ".provenance-tree-item"
  key_links:
    - from: "static/js/classify.js"
      to: "static/css/evidence.css"
      via: "renderProvenanceSection generates HTML using .provenance-tree classes"
      pattern: "provenance-tree"
    - from: "static/js/classify.js"
      to: "provenance_map"
      via: "API response contains provenance data per group_code"
      pattern: "provenanceMap\\[rec\\.group_code\\]"
---

<objective>
Upgrade the provenance section rendering from a flat layout to an expandable tree structure matching the CSS already in evidence.css and the user decision for "expandable tree/breadcrumb: Recommendation -> Definition -> TBS Page".

Purpose: The CSS for `.provenance-tree`, `.provenance-tree-item`, `.provenance-expandable`, and `.provenance-expand-btn` already exists in evidence.css (committed in 17-03). But classify.js `renderProvenanceSection()` currently renders a flat `.provenance-section` with simple paragraphs. This plan closes the gap so the provenance chain is displayed as a three-level expandable tree with ARIA compliance.

Output: Updated renderProvenanceSection() in classify.js that generates tree-structured HTML using the existing provenance-tree CSS classes.
</objective>

<execution_context>
@C:\Users\Administrator\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrator\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ui-layer/17-CONTEXT.md

# Existing code to modify
@static/js/classify.js

# CSS classes already available (DO NOT modify this file)
@static/css/evidence.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade renderProvenanceSection to expandable tree (EVD-01)</name>
  <files>static/js/classify.js</files>
  <action>
Replace the `renderProvenanceSection()` function (approximately lines 630-663 of classify.js) with a new implementation that renders an expandable provenance tree. Keep the same function signature: `renderProvenanceSection(provenance, groupCode, fallbackUrl)`.

The new implementation must:

1. Render a three-level provenance tree using the `.provenance-tree` CSS classes from evidence.css:
   - Level 1: Recommendation (group code + name, with confidence)
   - Level 2: TBS Group Definition (source_type from provenance object)
   - Level 3: Authoritative Source (TBS link with scraped_at date)

2. Use this HTML structure for each tree item:
```html
<div class="provenance-tree-item">
    <span class="provenance-tree-icon"><i class="fas fa-icon"></i></span>
    <div class="provenance-tree-content">
        <span class="provenance-tree-label">Label:</span>
        <span class="provenance-tree-value">Value</span>
    </div>
</div>
```

3. Make the definition level expandable using the `.provenance-expandable` CSS:
   - Add a button with class `provenance-expand-btn` that toggles `aria-expanded` on the parent `.provenance-expandable` div
   - The detail content uses class `provenance-detail-content` (CSS handles show/hide via `[aria-expanded="true"]`)
   - Include the full group definition text (from provenance.definition if available, otherwise show group code description from OCCUPATIONAL_GROUP_NAMES)
   - Bind click handler on the expand button to toggle aria-expanded between "true" and "false"

4. For the TBS source link level:
   - Use `target="_blank" rel="noopener noreferrer"` per existing pattern (Claude's discretion on link behavior: new tab)
   - Add anchor to TBS definitions page: append `#${groupCode.toLowerCase()}` if URL contains `definitions.html` and no anchor exists (preserve existing logic)
   - Show scraped date as `.provenance-tree-meta` below the link (Claude's discretion on archive date visibility: show it)

5. Icons for each level (using existing Font Awesome):
   - Level 1 (Recommendation): `fa-star`
   - Level 2 (Definition): `fa-book`
   - Level 3 (TBS Source): `fa-external-link-alt`

6. Wrap everything in a `<div class="detail-section">` with heading matching existing pattern:
```html
<div class="detail-section">
    <h4><i class="fas fa-sitemap"></i> Source Provenance</h4>
    <div class="provenance-tree">
        <!-- tree items here -->
    </div>
</div>
```

7. If no URL is available (provenance?.url and fallbackUrl are both falsy), return empty string (preserve existing guard).

IMPORTANT: Do NOT modify any other function in classify.js. Only replace renderProvenanceSection(). The rest of the file must remain identical.
  </action>
  <verify>
1. Open classify.js and confirm renderProvenanceSection generates HTML with class="provenance-tree"
2. Confirm the function still accepts (provenance, groupCode, fallbackUrl) parameters
3. Confirm expandable button has aria-expanded attribute
4. Confirm TBS link has target="_blank" rel="noopener noreferrer"
5. Confirm no other functions in classify.js were modified
6. Start Flask app with `python -m flask --app src.app run --debug` and navigate to Step 5 classification results -- expand a card and verify provenance section renders as a tree with three levels
  </verify>
  <done>
renderProvenanceSection() generates an expandable three-level provenance tree (Recommendation -> Definition -> TBS Source) using the .provenance-tree CSS classes from evidence.css. The definition level is expandable with aria-expanded toggling. TBS link opens in new tab. Scraped date shown as metadata.
  </done>
</task>

</tasks>

<verification>
1. Expand a recommendation card -- provenance section shows three-level tree with connecting lines (from CSS)
2. Click expand button on definition level -- definition detail expands with aria-expanded="true"
3. Click again -- collapses with aria-expanded="false"
4. TBS link opens in new tab
5. Scraped date visible below TBS link
6. All existing card functionality (confidence bars, evidence links, rationale) still works
</verification>

<success_criteria>
- Provenance section renders as expandable tree, not flat paragraphs
- Three levels visible: Recommendation, Definition, TBS Source
- Expand/collapse works with proper ARIA attributes
- No regressions in other classify.js functionality
</success_criteria>

<output>
After completion, create `.planning/phases/20-evidence-provenance/20-01-SUMMARY.md`
</output>
