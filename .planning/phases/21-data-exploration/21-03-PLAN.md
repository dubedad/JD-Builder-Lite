---
phase: 21-data-exploration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/labels_loader.py
  - src/vocabulary/index.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "labels_loader.py emits logger.warning() on every parquet/CSV load failure path"
    - "vocabulary/index.py emits logger.warning() before raising FileNotFoundError and on read exceptions"
    - "Both files still function identically after changes -- no behavioral regression"
  artifacts:
    - path: "src/services/labels_loader.py"
      provides: "Warning-logged parquet/CSV load failures"
      contains: "logger.warning"
    - path: "src/vocabulary/index.py"
      provides: "Warning-logged parquet load failures"
      contains: "logger.warning"
  key_links:
    - from: "src/services/labels_loader.py"
      to: "logging module"
      via: "import logging; logger = logging.getLogger(__name__)"
      pattern: "logger\\.warning"
    - from: "src/vocabulary/index.py"
      to: "logging module"
      via: "import logging; logger = logging.getLogger(__name__)"
      pattern: "logger\\.warning"
---

<objective>
Close the single verification gap in Phase 21 (Truth 4 -- PARTIAL): add logger.warning() calls to parquet/CSV load failure paths in labels_loader.py and vocabulary/index.py so no data load failure is silently swallowed.

Purpose: DATA-04 requires that any parquet file that fails to load produces a visible warning log entry. parquet_reader.py already does this correctly, but labels_loader.py and vocabulary/index.py read parquet directly and do not emit logger.warning() on failure. This plan adds minimal logging to their existing exception handlers WITHOUT rewriting or migrating them to parquet_reader.py.

Output: Both files emit logger.warning() on every failure path. Phase 21 Truth 4 fully satisfied.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-data-exploration/21-VERIFICATION.md
@src/services/labels_loader.py
@src/vocabulary/index.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logger.warning() to labels_loader.py failure paths</name>
  <files>src/services/labels_loader.py</files>
  <action>
Add `import logging` at the top of the file (after existing imports) and create a module-level logger:

```python
import logging
logger = logging.getLogger(__name__)
```

Then add `logger.warning(...)` calls to ALL failure paths in the `_load_*` methods. There are 8 load methods total (6 parquet, 2 CSV). The pattern for each method is:

1. **`_load_labels`** (3 failure paths):
   - Line 149 (no pandas): Add `logger.warning("Labels load skipped: pandas/pyarrow not installed")` before setting `self._load_error`
   - Line 153 (file not found): Add `logger.warning("Labels file not found: %s", self.LABELS_FILE)` before setting `self._load_error`
   - Line 161 (except block): Add `logger.warning("Failed to load labels from %s: %s", self.LABELS_FILE, e)` before setting `self._load_error`

2. **`_load_example_titles`** (3 failure paths): Same pattern as _load_labels but for EXAMPLE_TITLES_FILE. Add logger.warning() before each `self._load_error` assignment or `return False`.

3. **`_load_exclusions`** (3 failure paths -- currently bare `return False`):
   - No-pandas check: Add `logger.warning("Exclusions load skipped: pandas/pyarrow not installed")`
   - File not found: Add `logger.warning("Exclusions file not found: %s", self.EXCLUSIONS_FILE)`
   - Except block: Add `logger.warning("Failed to load exclusions from %s: %s", self.EXCLUSIONS_FILE, e)` -- also change `except Exception:` to `except Exception as e:` to capture the exception

4. **`_load_employment_reqs`** (3 failure paths): Same pattern as _load_exclusions. Fix bare `except Exception:` to `except Exception as e:`.

5. **`_load_workplaces`** (3 failure paths): Same pattern. Fix bare `except Exception:` to `except Exception as e:`.

6. **`_load_work_context`** (3 failure paths): Same pattern. Fix bare `except Exception:` to `except Exception as e:`.

7. **`_load_interests`** (3 failure paths -- CSV, not parquet, but still data load): Same pattern. Fix bare `except Exception:` to `except Exception as e:`.

8. **`_load_personal_attrs`** (3 failure paths -- CSV): Same pattern. Fix bare `except Exception:` to `except Exception as e:`.

IMPORTANT constraints:
- Do NOT change return values, control flow, or method signatures
- Do NOT rename or restructure any methods
- Do NOT migrate to parquet_reader.py -- that is out of scope
- Keep existing `print()` statements on success paths (they are informational, not errors)
- Keep existing `self._load_error` assignments (they serve a different purpose -- runtime error inspection)
- The logger.warning() calls are ADDITIVE ONLY -- add them alongside existing error handling, do not replace anything
  </action>
  <verify>
Run: `cd /Users/victornishi/Documents/GitHub/JD-Builder2/JD-Builder-Lite && python3 -c "import src.services.labels_loader; print('import OK')"`

Then verify logger.warning exists on failure paths:
`grep -n "logger.warning" src/services/labels_loader.py | wc -l` should return at least 16 (2 warnings per load method across 8 methods, minimum -- some methods have 3 failure paths)

Then verify the app still starts:
`cd /Users/victornishi/Documents/GitHub/JD-Builder2/JD-Builder-Lite && PYTHONPATH=. timeout 5 python3 -c "from src.services.labels_loader import labels_loader; print('singleton OK, available:', labels_loader.is_available())"`
  </verify>
  <done>Every _load_* method in labels_loader.py emits logger.warning() on every failure path (no-pandas, file-not-found, exception). Existing behavior unchanged -- same return values, same self._load_error assignments, same print() on success.</done>
</task>

<task type="auto">
  <name>Task 2: Add logger.warning() to vocabulary/index.py failure paths</name>
  <files>src/vocabulary/index.py</files>
  <action>
Add `import logging` at the top of the file (after existing imports) and create a module-level logger:

```python
import logging
logger = logging.getLogger(__name__)
```

Then add `logger.warning(...)` calls to the failure paths in `_load_vocabulary`:

1. **FileNotFoundError raise (line 61-64)**: Add `logger.warning("Required vocabulary file not found: %s (full path: %s)", filename, filepath)` BEFORE the `raise FileNotFoundError(...)`. The raise still happens -- we just log first.

2. **pd.read_parquet potential exception (line 67)**: Wrap the `pd.read_parquet(filepath)` call in a try/except that logs and re-raises:

```python
try:
    df = pd.read_parquet(filepath)
except Exception as e:
    logger.warning("Failed to read vocabulary file %s: %s", filepath, e)
    raise
```

This ensures any parquet read error (corrupt file, schema mismatch, pyarrow error) is logged before the exception propagates. The `raise` without arguments preserves the original traceback.

3. **Empty vocabulary ValueError (line 88-92)**: Add `logger.warning("Vocabulary loaded but is empty -- parquet files may have no valid column names")` BEFORE the `raise ValueError(...)`.

IMPORTANT constraints:
- Do NOT change the exception types raised (FileNotFoundError, ValueError)
- Do NOT suppress exceptions -- they must still propagate
- Do NOT change the constructor or reload() interface
- Do NOT add any new dependencies beyond `logging`
  </action>
  <verify>
Run: `cd /Users/victornishi/Documents/GitHub/JD-Builder2/JD-Builder-Lite && python3 -c "import src.vocabulary.index; print('import OK')"`

Then verify logger.warning exists:
`grep -n "logger.warning" src/vocabulary/index.py | wc -l` should return at least 3

Then verify behavior is preserved -- a missing-file scenario should still raise FileNotFoundError:
`cd /Users/victornishi/Documents/GitHub/JD-Builder2/JD-Builder-Lite && PYTHONPATH=. python3 -c "
try:
    from src.vocabulary.index import VocabularyIndex
    v = VocabularyIndex('/nonexistent/path')
except FileNotFoundError as e:
    print('FileNotFoundError still raised: OK')
except Exception as e:
    print(f'Wrong exception type: {type(e).__name__}: {e}')
"`
  </verify>
  <done>vocabulary/index.py emits logger.warning() before every FileNotFoundError raise, on pd.read_parquet() failure, and before ValueError raise. All exceptions still propagate unchanged. No behavioral regression.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify DATA-04 is satisfied:

1. `grep -c "logger.warning" src/services/labels_loader.py` -- expect 16+ (at least 2 per load method x 8 methods)
2. `grep -c "logger.warning" src/vocabulary/index.py` -- expect 3+
3. `grep -c "import logging" src/services/labels_loader.py` -- expect 1
4. `grep -c "import logging" src/vocabulary/index.py` -- expect 1
5. App starts without error: `cd /Users/victornishi/Documents/GitHub/JD-Builder2/JD-Builder-Lite && PYTHONPATH=. timeout 5 python3 -c "from src.services.labels_loader import labels_loader; print('OK')"`
6. No parquet load failure is silently swallowed -- every `return False` or `raise` in a data-load method is preceded by a `logger.warning()` call
</verification>

<success_criteria>
- labels_loader.py has `import logging` and `logger = logging.getLogger(__name__)` at module level
- Every `_load_*` method in labels_loader.py calls `logger.warning()` on every failure path
- vocabulary/index.py has `import logging` and `logger = logging.getLogger(__name__)` at module level
- Every failure path in `_load_vocabulary` calls `logger.warning()` before raising or returning
- Both files import and execute without error
- No existing behavior changed (return values, exceptions raised, method signatures all identical)
- DATA-04 fully satisfied: no parquet/CSV load failure is silently swallowed
</success_criteria>

<output>
After completion, create `.planning/phases/21-data-exploration/21-03-SUMMARY.md`
</output>
