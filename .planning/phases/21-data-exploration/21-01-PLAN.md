---
phase: 21-data-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-data-exploration/DATA-INVENTORY.md
  - .planning/phases/21-data-exploration/GAP-ANALYSIS.md
autonomous: true

must_haves:
  truths:
    - "Developer can open DATA-INVENTORY.md and see every gold parquet file with column names, row counts, profile counts, and which OASIS data element it replaces"
    - "Developer can open GAP-ANALYSIS.md and see an explicit list of OASIS data fields that have no adequate parquet equivalent, with the reason for each gap"
    - "No gap is inferred -- every gap entry names the field, states whether parquet exists at all, and why it is insufficient"
  artifacts:
    - path: ".planning/phases/21-data-exploration/DATA-INVENTORY.md"
      provides: "Structured inventory of all 25 gold parquet files"
      contains: "element_labels.parquet"
    - path: ".planning/phases/21-data-exploration/GAP-ANALYSIS.md"
      provides: "Explicit gap analysis -- OASIS fields not covered by parquet"
      contains: "Main Duties"
  key_links: []
---

<objective>
Produce the DATA-INVENTORY.md and GAP-ANALYSIS.md documents that Phase 22 and Phase 23 developers will use to know which parquet files exist, what they contain, and where OASIS must remain.

Purpose: DATA-01 and DATA-02 require structured developer-facing documents. The research phase already inspected every file -- this plan formats that knowledge into permanent reference docs.
Output: Two markdown files in the phase directory.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-data-exploration/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DATA-INVENTORY.md from gold parquet files</name>
  <files>.planning/phases/21-data-exploration/DATA-INVENTORY.md</files>
  <action>
Read every parquet file in `/Users/victornishi/Documents/GitHub/JobForge-2.0/data/gold/` using pandas. For each file, record:

1. File name
2. Column names (list all)
3. Row count
4. Distinct profile count (count unique values in `oasis_profile_code` or `noc_code` column, whichever exists; if neither, note "no profile key")
5. OASIS mapping: which OASIS data element this file replaces or supplements (use the research findings to map)

Organize the inventory into three sections:
- **Profile Data Files** (element_* and oasis_* files that map to profile tabs)
- **Reference/Dimensional Files** (dim_*, job_architecture, bridge_*, fact_*)
- **Labour Market Files** (cops_* files)

For each file entry, use this format:
```
### {filename}
- **Rows:** {N}
- **Profiles:** {N distinct} (column: {key_column})
- **Columns:** {comma-separated list}
- **OASIS mapping:** {which OASIS section this replaces, or "N/A - reference data"}
- **Notes:** {any issues like whitespace columns, low coverage, etc.}
```

Add a summary table at the top with file name, rows, profiles, and OASIS mapping for quick scanning.

Use Python with pandas to read the files -- run `import pandas as pd; df = pd.read_parquet(path)` for each. Handle any read errors gracefully and note them.
  </action>
  <verify>
The file `.planning/phases/21-data-exploration/DATA-INVENTORY.md` exists, contains entries for all 25 gold parquet files, and each entry has columns, row count, and profile count.
Run: `grep -c "###" .planning/phases/21-data-exploration/DATA-INVENTORY.md` -- should return 25 (one heading per file).
  </verify>
  <done>DATA-INVENTORY.md lists all 25 gold parquet files with schema, row counts, profile counts, and OASIS mapping. DATA-01 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Create GAP-ANALYSIS.md from inventory findings</name>
  <files>.planning/phases/21-data-exploration/GAP-ANALYSIS.md</files>
  <action>
Create GAP-ANALYSIS.md that explicitly lists every OASIS data field that has NO adequate parquet equivalent. This is the document that tells Phase 22 and 23 developers which sections must continue using OASIS scraping.

Structure:

1. **Summary** -- one paragraph stating how many fields are covered vs gapped
2. **Gap Table** -- for each gapped field:

| OASIS Field | Parquet File Exists? | Why It's a Gap | Fallback Strategy |
|-------------|---------------------|----------------|-------------------|
| Main Duties / Key Activities | Yes (element_main_duties.parquet) | Only 8 rows / 3 profiles; source CSV has 4,991 rows / 900 profiles -- ETL incomplete | OASIS scraping (permanent until JobForge ETL runs) |
| Interests / Holland Codes | No gold parquet | Only exists in source CSV; not in gold tier | OASIS scraping via LabelsLoader |
| Personal Attributes | No gold parquet | Only exists in source CSV; not in gold tier | OASIS scraping via LabelsLoader |
| Core Competencies | No data at all | No parquet, no CSV | OASIS scraping |
| Career Mobility | No data at all | No parquet, no CSV | OASIS scraping |

3. **Covered Fields** -- table listing every field that IS covered by parquet (confirming coverage, not just listing gaps):

| Field | Parquet File | Profiles Covered | Status |
|-------|-------------|------------------|--------|
| Skills | oasis_skills.parquet | 900/900 | Full coverage |
| Abilities | oasis_abilities.parquet | 900/900 | Full coverage |
| ... etc |

4. **Profile Code Format Note** -- document the `oasis_profile_code` format ('21211.00') vs NOC code format ('21211') and that Phase 22 must handle the transformation.

5. **Column Whitespace Warning** -- note which files have whitespace-contaminated column names (from research: abilities has 14, skills has 6, etc.) and that all parquet column names must be stripped before use.

Do NOT infer gaps. Every gap must be named and the reason stated explicitly. Use the research findings as the authoritative source.
  </action>
  <verify>
The file `.planning/phases/21-data-exploration/GAP-ANALYSIS.md` exists and contains:
- A gap table with at least 5 entries (Main Duties, Interests, Personal Attributes, Core Competencies, Career Mobility)
- A covered fields table with at least 5 entries (Skills, Abilities, Knowledge, Work Activities, Work Context)
- Profile code format note
- Column whitespace warning
Run: `grep -c "OASIS" .planning/phases/21-data-exploration/GAP-ANALYSIS.md` -- should be > 10 (OASIS referenced throughout).
  </verify>
  <done>GAP-ANALYSIS.md explicitly names every gapped field with reason and fallback strategy. No gap is inferred. DATA-02 is satisfied.</done>
</task>

</tasks>

<verification>
- DATA-INVENTORY.md exists with 25 file entries
- GAP-ANALYSIS.md exists with explicit gap table and covered fields table
- Both documents reference actual parquet file inspection, not assumptions
- Profile code format difference is documented
- Column whitespace issue is documented
</verification>

<success_criteria>
- DATA-01: Structured inventory document exists with schema, row counts, and OASIS mapping for every gold parquet file
- DATA-02: Gap analysis document explicitly names every OASIS field without parquet equivalent, with reason and fallback
</success_criteria>

<output>
After completion, create `.planning/phases/21-data-exploration/21-01-SUMMARY.md`
</output>
