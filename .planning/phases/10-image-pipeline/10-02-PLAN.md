---
phase: 10-image-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ps_careers_site/templates/base.html
  - ps_careers_site/static/css/gradients.css
autonomous: true
requirements: [IMG-05]

must_haves:
  truths:
    - "A card with null image_path displays a function-colored gradient background instead of a broken image"
    - "Each of the 22 job functions has a distinct muted gradient"
    - "Family and title cards with null image_path inherit their parent function's gradient color"
    - "Gradient is pure CSS — no JavaScript, no HTTP requests"
    - "Unsplash attribution credit appears in the site footer"
  artifacts:
    - path: "ps_careers_site/static/css/gradients.css"
      provides: "22 per-function gradient CSS classes"
      min_lines: 50
      contains: "gradient-administration"
    - path: "ps_careers_site/templates/base.html"
      provides: "Unsplash attribution in footer"
      contains: "Unsplash"
  key_links:
    - from: "ps_careers_site/static/css/gradients.css"
      to: "ps_careers_site/templates/ (Phase 11 card templates)"
      via: "CSS class gradient-{slug} applied in Jinja2 template"
      pattern: "\\.gradient-[a-z]"
---

<objective>
Create the 22 per-function gradient fallback CSS classes and add Unsplash attribution to the site footer.

Purpose: When a card has no image (image_path is NULL), it must display a styled gradient background using the entity's job function color — not a broken image or a generic single-color fallback. This ensures the site looks polished even before or without the image pipeline running. Additionally, Unsplash API terms require attribution (per D-04).

Output: `static/css/gradients.css` (new file) + updated `base.html` footer with Unsplash credit + CSS link
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-image-pipeline/10-CONTEXT.md
@.planning/phases/10-image-pipeline/10-RESEARCH.md

@ps_careers_site/templates/base.html
@ps_careers_site/templates/careers.html

<interfaces>
<!-- From careers.html — existing card-no-image pattern (line 68-70): -->
.card-no-image {
    background: linear-gradient(160deg, #1a3a5c 0%, #26374a 60%, #1c2e40 100%);
}

<!-- Card template pattern (careers.html lines 177-184): -->
{% if family.image_file %}
<div class="card-inner" style="background-image: ...url(...);">
{% else %}
<div class="card-inner card-no-image">
{% endif %}

<!-- Phase 11 will update the template to use gradient-{slug} classes like: -->
<!-- <div class="card-inner card-no-image gradient-{{ entity.job_function_slug }}"> -->
<!-- This plan creates the CSS classes Phase 11 will consume -->

<!-- 22 function slugs from job_functions table (Phase 9): -->
administration
artificial-intelligence-ai
arts-and-social-sciences
audit-and-evaluation
commerce-retail-services-and-insurance
communications
education
engineering-and-architecture
environmental-services
financial-management
general-labour-and-trades
health-care-and-social-services
human-resources-management
information-management
information-technology
legal-services
public-safety-defence-and-security-management
real-property-management
relationship-management
science-and-math
supply-chain-management
transportation

<!-- base.html footer structure: -->
<!-- footer-grid (line 38): 5-col grid with link sections -->
<!-- footer-bottom (line 80): logo + social icons + terms links -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-function gradient CSS file</name>
  <files>ps_careers_site/static/css/gradients.css</files>
  <read_first>
    - ps_careers_site/templates/careers.html (existing card-no-image class definition and card template pattern)
    - .planning/phases/10-image-pipeline/10-RESEARCH.md (gradient palette table with exact hex values per function slug)
  </read_first>
  <action>
    Create NEW file `ps_careers_site/static/css/gradients.css` containing:

    1. **Base fallback class** (override for existing `.card-no-image`):
       ```css
       /* Per-function gradient fallback for cards without images.
          Applied as: class="card-inner card-no-image gradient-{function-slug}"
          Phase 11 templates inject the slug from DB context.
          All colors are muted/desaturated per D-10 (federal government palette). */
       ```

    2. **22 gradient classes** — one per function slug. Each uses `linear-gradient(160deg, {from} 0%, {to} 100%)` to match the existing card-no-image angle (160deg).

       Exact values from RESEARCH.md palette (per D-09, D-10):

       ```css
       .gradient-administration { background: linear-gradient(160deg, #2d3e50 0%, #1a2633 100%); }
       .gradient-artificial-intelligence-ai { background: linear-gradient(160deg, #1e3a4a 0%, #0d2233 100%); }
       .gradient-arts-and-social-sciences { background: linear-gradient(160deg, #3a3050 0%, #22203a 100%); }
       .gradient-audit-and-evaluation { background: linear-gradient(160deg, #2c3a2c 0%, #1a251a 100%); }
       .gradient-commerce-retail-services-and-insurance { background: linear-gradient(160deg, #3a2e20 0%, #261e14 100%); }
       .gradient-communications { background: linear-gradient(160deg, #1e3040 0%, #0d1f2d 100%); }
       .gradient-education { background: linear-gradient(160deg, #2a3a28 0%, #1a2618 100%); }
       .gradient-engineering-and-architecture { background: linear-gradient(160deg, #253040 0%, #151e2a 100%); }
       .gradient-environmental-services { background: linear-gradient(160deg, #263828 0%, #162418 100%); }
       .gradient-financial-management { background: linear-gradient(160deg, #2a3020 0%, #1a2010 100%); }
       .gradient-general-labour-and-trades { background: linear-gradient(160deg, #303828 0%, #202618 100%); }
       .gradient-health-care-and-social-services { background: linear-gradient(160deg, #1e3038 0%, #0e2028 100%); }
       .gradient-human-resources-management { background: linear-gradient(160deg, #2e3040 0%, #1e2030 100%); }
       .gradient-information-management { background: linear-gradient(160deg, #203840 0%, #122830 100%); }
       .gradient-information-technology { background: linear-gradient(160deg, #1a2840 0%, #0e1c30 100%); }
       .gradient-legal-services { background: linear-gradient(160deg, #2a2c3c 0%, #1a1c2c 100%); }
       .gradient-public-safety-defence-and-security-management { background: linear-gradient(160deg, #252830 0%, #151820 100%); }
       .gradient-real-property-management { background: linear-gradient(160deg, #303028 0%, #201e18 100%); }
       .gradient-relationship-management { background: linear-gradient(160deg, #2c2838 0%, #1c1828 100%); }
       .gradient-science-and-math { background: linear-gradient(160deg, #202c3a 0%, #101c2a 100%); }
       .gradient-supply-chain-management { background: linear-gradient(160deg, #2e3028 0%, #1e2018 100%); }
       .gradient-transportation { background: linear-gradient(160deg, #28303a 0%, #18202a 100%); }
       ```

    Per D-11: CSS-only, no JS, no HTTP requests. These classes are consumed by Jinja2 templates in Phase 11 via `class="card-inner card-no-image gradient-{{ entity.job_function_slug }}"`.

    Create the `static/css/` directory if it does not exist (it likely does not — current CSS is inline in templates).
  </action>
  <verify>
    <automated>cd ps_careers_site && test -f static/css/gradients.css && grep -c "\.gradient-" static/css/gradients.css</automated>
  </verify>
  <acceptance_criteria>
    - ps_careers_site/static/css/gradients.css exists
    - File contains exactly 22 lines matching `.gradient-{slug}` pattern
    - File contains `.gradient-administration`
    - File contains `.gradient-transportation` (last slug alphabetically)
    - File contains `.gradient-artificial-intelligence-ai` (slug with hyphens)
    - File contains `.gradient-public-safety-defence-and-security-management` (longest slug)
    - Every gradient uses `linear-gradient(160deg,` (matching existing card-no-image angle)
    - No JavaScript in the file
    - All hex values are dark (first char after # is 0-3 range) per D-10 muted palette
    - `grep -c "\.gradient-" static/css/gradients.css` outputs `22`
  </acceptance_criteria>
  <done>22 per-function gradient CSS classes exist in a standalone file, each with muted/desaturated dark colors appropriate for a federal government site. Ready for Phase 11 templates to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Add gradient CSS link and Unsplash attribution to base template</name>
  <files>ps_careers_site/templates/base.html</files>
  <read_first>
    - ps_careers_site/templates/base.html (current footer structure — lines 37-114)
    - ps_careers_site/static/css/gradients.css (created in Task 1)
    - .planning/phases/10-image-pipeline/10-CONTEXT.md (D-04: Unsplash attribution requirement)
  </read_first>
  <action>
    Make TWO changes to `ps_careers_site/templates/base.html`:

    **Change 1: Add CSS link in head (after line 9, before existing style tag on line 10):**
    ```html
    <link rel="stylesheet" href="{{ url_for('static', path='css/gradients.css') }}">
    ```

    **Change 2: Add Unsplash attribution in footer-bottom div (line 80).**

    Insert a small credit text between the social icons div (ends ~line 105) and the Terms/Francais links div (starts ~line 106):
    ```html
    <span style="color:#666; font-size:11px; font-family:Roboto,sans-serif;">Photos by <a href="https://unsplash.com/?utm_source=dnd_civilian_careers&utm_medium=referral" style="color:#888; text-decoration:underline;">Unsplash</a></span>
    ```

    Per D-04: "Photos from Unsplash" credit in the footer satisfies Unsplash API guidelines for this phase. Per-image photographer attribution is deferred.

    The `footer-bottom` div uses `display:flex; align-items:center; justify-content:space-between; flex-wrap:wrap; gap:16px;` — the new span will sit naturally between existing flex children.
  </action>
  <verify>
    <automated>cd ps_careers_site && grep "gradients.css" templates/base.html && grep -i "unsplash" templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/base.html contains `gradients.css` in a link tag
    - templates/base.html contains `url_for('static', path='css/gradients.css')` (Jinja2 static URL)
    - templates/base.html contains the word `Unsplash` (attribution text)
    - templates/base.html contains `unsplash.com` (attribution link)
    - templates/base.html contains `utm_source=dnd_civilian_careers` (Unsplash referral tracking)
    - The link tag appears in the head section (before `</head>`)
    - The Unsplash attribution appears inside the footer (after `<footer>`)
  </acceptance_criteria>
  <done>base.html loads the gradient CSS file globally and displays "Photos by Unsplash" credit in the footer. All pages on the site now have access to the 22 gradient classes, and Unsplash attribution is satisfied per D-04.</done>
</task>

</tasks>

<verification>
1. `test -f ps_careers_site/static/css/gradients.css` — file exists
2. `grep -c "\.gradient-" ps_careers_site/static/css/gradients.css` — outputs 22
3. `grep "gradients.css" ps_careers_site/templates/base.html` — CSS linked in head
4. `grep -i "unsplash" ps_careers_site/templates/base.html` — attribution present in footer
5. `cd ps_careers_site && python -c "from fastapi.testclient import TestClient; from main import app; c=TestClient(app); r=c.get('/careers'); assert 'gradients.css' not in r.text or r.status_code==200; print('OK')"` — page loads without error (gradients.css linked even if not yet consumed by card templates)
</verification>

<success_criteria>
- 22 distinct gradient CSS classes exist, one per job function slug
- All gradient colors are dark and muted (federal palette per D-10)
- Gradient CSS file is linked in base.html head (available on all pages)
- Unsplash "Photos by Unsplash" attribution appears in site footer (per D-04)
- No JavaScript involved in gradient rendering (per D-11)
- Phase 11 can consume gradient classes by adding `gradient-{{ entity.job_function_slug }}` to card markup
</success_criteria>

<output>
After completion, create `.planning/phases/10-image-pipeline/10-02-SUMMARY.md`
</output>
