---
phase: 11-provenance-architecture
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/models/styled_content.py
  - src/models/__init__.py
autonomous: true

must_haves:
  truths:
    - "Styled output links to original NOC statement ID"
    - "Original NOC text is always preserved alongside styled variant"
    - "Generation metadata includes confidence score, retry count, timestamp"
    - "Version history tracks all generation attempts for reversion"
  artifacts:
    - path: "src/models/styled_content.py"
      provides: "StyledStatement, GenerationAttempt, StyleVersionHistory models"
      exports: ["StyledStatement", "GenerationAttempt", "StyleVersionHistory"]
    - path: "src/models/__init__.py"
      provides: "Package exports for styled content models"
      contains: "StyledStatement"
  key_links:
    - from: "StyledStatement"
      to: "StyleContentType"
      via: "content_type field"
      pattern: "content_type.*StyleContentType"
    - from: "StyledStatement"
      to: "VocabularyAudit"
      via: "vocabulary_audit field"
      pattern: "vocabulary_audit.*VocabularyAudit"
    - from: "StyleVersionHistory"
      to: "GenerationAttempt"
      via: "generation_attempts list"
      pattern: "generation_attempts.*List\\[GenerationAttempt\\]"
---

<objective>
Create styled content models: StyledStatement for linking styled output to NOC source, GenerationAttempt for tracking individual generation tries, and StyleVersionHistory for maintaining reversion capability.

Purpose: Complete the provenance schema with models that track the full lifecycle of styled content - from original NOC statement through multiple generation attempts to the currently active version.

Output: New styled_content.py module with full provenance models ready for Phase 12 generation implementation.
</objective>

<execution_context>
@C:\Users\Administrator\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrator\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase context with user decisions
@.planning/phases/11-provenance-architecture/11-CONTEXT.md
@.planning/phases/11-provenance-architecture/11-RESEARCH.md

# Plan 01 output - must be complete first
@.planning/phases/11-provenance-architecture/11-01-SUMMARY.md

# Foundation models from Plan 01
@src/models/ai.py
@src/models/vocabulary_audit.py

# Existing export models for pattern reference
@src/models/export_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StyledStatement and GenerationAttempt models</name>
  <files>src/models/styled_content.py</files>
  <action>
Create new module src/models/styled_content.py with:

1. **GenerationAttempt model** - Single generation attempt record (PROV-05):
   - attempt_id: str (UUID, use default_factory with uuid4)
   - styled_text: str
   - confidence_score: float (0.0 to 1.0, use Field with ge/le constraints)
   - vocabulary_coverage: float (percentage)
   - vocabulary_audit: VocabularyAudit (from Plan 01)
   - generated_at: datetime
   - was_accepted: bool = False (whether user accepted this version)
   - Use frozen=True for immutability

2. **StyledStatement model** - Styled variant with full provenance (PROV-01, PROV-03):
   - original_noc_statement_id: str (links to source NOC - PROV-01)
   - original_noc_text: str (preserved original - PROV-03)
   - styled_text: str (the styled variant)
   - content_type: StyleContentType (from Plan 01 - PROV-02)
   - confidence_score: float (0.0 to 1.0)
   - retry_count: int (Field with ge=0, default=0)
   - vocabulary_coverage: float (percentage)
   - vocabulary_audit: VocabularyAudit
   - generated_at: datetime
   - version_id: str (UUID for tracking)
   - Use frozen=True for immutability

Imports needed:
```python
from datetime import datetime
from typing import Optional
from uuid import uuid4
from pydantic import BaseModel, Field, ConfigDict

from src.models.ai import StyleContentType
from src.models.vocabulary_audit import VocabularyAudit
```

Include module docstring explaining purpose (styled content provenance models for Phase 12).
  </action>
  <verify>
Run: `python -c "from src.models.styled_content import StyledStatement, GenerationAttempt; from src.models.ai import StyleContentType; from src.models.vocabulary_audit import VocabularyAudit; from datetime import datetime; audit = VocabularyAudit(noc_terms_used=[], non_noc_terms=[], coverage_percentage=100.0, total_content_words=10, noc_word_count=10); stmt = StyledStatement(original_noc_statement_id='key_activities-0', original_noc_text='Analyze data', styled_text='Analyzes data to support decision-making.', content_type=StyleContentType.AI_STYLED, confidence_score=0.9, retry_count=0, vocabulary_coverage=100.0, vocabulary_audit=audit, generated_at=datetime.now(), version_id='v1'); print(f'Statement linked to: {stmt.original_noc_statement_id}')"`
Expected: Prints "Statement linked to: key_activities-0"
  </verify>
  <done>
StyledStatement model links styled output to original NOC statement ID with preserved original text, GenerationAttempt tracks individual generation metadata
  </done>
</task>

<task type="auto">
  <name>Task 2: Add StyleVersionHistory model</name>
  <files>src/models/styled_content.py</files>
  <action>
Add StyleVersionHistory model to src/models/styled_content.py (after GenerationAttempt and StyledStatement):

**StyleVersionHistory model** - Complete version history for reversion (per CONTEXT.md):
- original_noc_statement_id: str (the source statement this history tracks)
- original_noc_text: str (preserved NOC text)
- generation_attempts: List[GenerationAttempt] = Field(default_factory=list)
- active_version_index: Optional[int] = None (index into generation_attempts, None if no accepted version)

This model is NOT frozen because:
- generation_attempts list grows as new versions are generated
- active_version_index changes when user selects different version

Per CONTEXT.md:
- "Full version history tracked (all generated variants kept for audit)"
- "Users can revert to any previous styled version from history"

Research recommends 10 versions max per statement, but enforce that in Phase 12 logic, not schema.
  </action>
  <verify>
Run: `python -c "from src.models.styled_content import StyleVersionHistory, GenerationAttempt; from src.models.vocabulary_audit import VocabularyAudit; from datetime import datetime; audit = VocabularyAudit(noc_terms_used=[], non_noc_terms=[], coverage_percentage=95.0, total_content_words=20, noc_word_count=19); attempt = GenerationAttempt(styled_text='Test styled.', confidence_score=0.85, vocabulary_coverage=95.0, vocabulary_audit=audit, generated_at=datetime.now()); history = StyleVersionHistory(original_noc_statement_id='skills-0', original_noc_text='Knowledge of policy'); history.generation_attempts.append(attempt); history.active_version_index = 0; print(f'History has {len(history.generation_attempts)} attempt(s), active index: {history.active_version_index}')"`
Expected: Prints "History has 1 attempt(s), active index: 0"
  </verify>
  <done>
StyleVersionHistory tracks all generation attempts with ability to set active version for reversion
  </done>
</task>

<task type="auto">
  <name>Task 3: Update models __init__.py with styled content exports</name>
  <files>src/models/__init__.py</files>
  <action>
Update src/models/__init__.py to export styled content models:

Add imports:
```python
from src.models.styled_content import (
    StyledStatement,
    GenerationAttempt,
    StyleVersionHistory,
)
```

Add to __all__ list (if exists):
- StyledStatement
- GenerationAttempt
- StyleVersionHistory

Verify no circular imports by running the import test.
  </action>
  <verify>
Run: `python -c "from src.models import StyledStatement, GenerationAttempt, StyleVersionHistory, StyleContentType, VocabularyAudit; print('All Phase 11 models importable from src.models')"`
Expected: No import errors, prints confirmation
  </verify>
  <done>
All styled content models exportable from src.models package alongside Plan 01 types
  </done>
</task>

</tasks>

<verification>
All tasks complete when:
1. `python -c "from src.models.styled_content import StyledStatement; print(StyledStatement.model_fields.keys())"` shows all required fields
2. `python -c "from src.models.styled_content import StyleVersionHistory; h = StyleVersionHistory(original_noc_statement_id='x', original_noc_text='y'); print(type(h.generation_attempts))"` shows list type
3. `python -c "from src.models import StyledStatement, GenerationAttempt, StyleVersionHistory"` imports without error
</verification>

<success_criteria>
- StyledStatement links styled output to original_noc_statement_id (PROV-01)
- StyledStatement preserves original_noc_text alongside styled variant (PROV-03)
- StyledStatement includes content_type for AI disclosure differentiation (PROV-02)
- StyledStatement includes vocabulary_audit with coverage data (PROV-04)
- GenerationAttempt tracks confidence_score, retry implicit via count, generated_at (PROV-05)
- StyleVersionHistory enables version reversion with generation_attempts list
- All models importable from src.models package
</success_criteria>

<output>
After completion, create `.planning/phases/11-provenance-architecture/11-02-SUMMARY.md`
</output>
