---
phase: 11-provenance-architecture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/ai.py
  - src/models/vocabulary_audit.py
autonomous: true

must_haves:
  truths:
    - "StyleContentType enum distinguishes AI_STYLED, AI_GENERATED, ORIGINAL_NOC"
    - "VocabularyAudit tracks NOC terms used and coverage percentage"
    - "Confidence thresholds define green/yellow/red levels"
  artifacts:
    - path: "src/models/ai.py"
      provides: "StyleContentType enum"
      contains: "class StyleContentType"
    - path: "src/models/vocabulary_audit.py"
      provides: "VocabularyTerm and VocabularyAudit models"
      exports: ["VocabularyTerm", "VocabularyAudit", "ConfidenceLevel", "CONFIDENCE_THRESHOLDS"]
  key_links:
    - from: "src/models/vocabulary_audit.py"
      to: "VocabularyValidator output"
      via: "matching field structure"
      pattern: "non_noc_terms.*List\\[str\\]"
---

<objective>
Create foundation provenance models: StyleContentType enum for AI disclosure differentiation and VocabularyAudit model for tracking NOC vocabulary usage in styled content.

Purpose: Establish the data types needed by styled content models (Plan 02) and eventual generation implementation (Phase 12). These types define how styled content is categorized and how vocabulary compliance is tracked.

Output: Two extended model files with enums, constants, and Pydantic models for vocabulary audit tracking.
</objective>

<execution_context>
@C:\Users\Administrator\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrator\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase context with user decisions
@.planning/phases/11-provenance-architecture/11-CONTEXT.md
@.planning/phases/11-provenance-architecture/11-RESEARCH.md

# Existing models to extend
@src/models/ai.py
@src/models/export_models.py

# Vocabulary validator for field alignment
@src/vocabulary/validator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StyleContentType enum to ai.py</name>
  <files>src/models/ai.py</files>
  <action>
Add StyleContentType enum to existing ai.py module (after imports, before classes).

Per CONTEXT.md AI Disclosure Labels:
- AI_STYLED = "ai_styled" -> labeled "AI-Styled using Job Description Samples"
- AI_GENERATED = "ai_generated" -> for synthesized content (e.g., overview)
- ORIGINAL_NOC = "original_noc" -> fallback when styling fails, marked "Original NOC"

Implementation:
```python
from enum import Enum

class StyleContentType(str, Enum):
    """Differentiated AI disclosure labels per PROV-02.

    AI_STYLED: Content styled from NOC using JD samples
    AI_GENERATED: Fully synthesized content (e.g., overview paragraphs)
    ORIGINAL_NOC: Unmodified NOC statement (fallback when styling fails)
    """
    AI_STYLED = "ai_styled"
    AI_GENERATED = "ai_generated"
    ORIGINAL_NOC = "original_noc"
```

Add Enum import if not present. Export in __all__ if module uses explicit exports.
  </action>
  <verify>
Run: `python -c "from src.models.ai import StyleContentType; print(list(StyleContentType))"`
Expected: Lists all three enum values
  </verify>
  <done>
StyleContentType enum exists with AI_STYLED, AI_GENERATED, ORIGINAL_NOC values
  </done>
</task>

<task type="auto">
  <name>Task 2: Create vocabulary_audit.py with VocabularyAudit models</name>
  <files>src/models/vocabulary_audit.py</files>
  <action>
Create new module src/models/vocabulary_audit.py with:

1. **ConfidenceLevel enum** - Visual indicator levels per CONTEXT.md:
   - HIGH = "green" (>= 0.8 confidence)
   - MEDIUM = "yellow" (>= 0.5 and < 0.8)
   - LOW = "red" (< 0.5)

2. **CONFIDENCE_THRESHOLDS dict** - Threshold values (Claude's discretion per CONTEXT.md):
   ```python
   CONFIDENCE_THRESHOLDS = {
       "high": 0.8,
       "medium": 0.5,
       "low": 0.0
   }
   ```

3. **VocabularyTerm model** - Individual term with category metadata:
   - term: str
   - category: str (skill, ability, knowledge, work_activity)
   - is_noc_term: bool
   - Use frozen=True for immutability

4. **VocabularyAudit model** - Vocabulary validation results (per PROV-04):
   - noc_terms_used: List[VocabularyTerm]
   - non_noc_terms: List[str] (matches VocabularyValidator output)
   - coverage_percentage: float
   - total_content_words: int
   - noc_word_count: int
   - Use frozen=True for immutability

Fields align with VocabularyValidator.validate_text() output structure.

Include module docstring explaining purpose (vocabulary audit for styled content provenance).
  </action>
  <verify>
Run: `python -c "from src.models.vocabulary_audit import VocabularyAudit, VocabularyTerm, ConfidenceLevel, CONFIDENCE_THRESHOLDS; print('Models loaded'); audit = VocabularyAudit(noc_terms_used=[], non_noc_terms=['test'], coverage_percentage=95.5, total_content_words=20, noc_word_count=19); print(f'Coverage: {audit.coverage_percentage}%')"`
Expected: Models load, can instantiate VocabularyAudit
  </verify>
  <done>
VocabularyAudit model exists with all required fields, ConfidenceLevel enum with green/yellow/red, and CONFIDENCE_THRESHOLDS constants
  </done>
</task>

<task type="auto">
  <name>Task 3: Update models __init__.py exports</name>
  <files>src/models/__init__.py</files>
  <action>
Update src/models/__init__.py to export new types:

Add imports:
```python
from src.models.ai import StyleContentType
from src.models.vocabulary_audit import (
    VocabularyTerm,
    VocabularyAudit,
    ConfidenceLevel,
    CONFIDENCE_THRESHOLDS,
)
```

Add to __all__ list (if exists):
- StyleContentType
- VocabularyTerm
- VocabularyAudit
- ConfidenceLevel
- CONFIDENCE_THRESHOLDS

If __all__ doesn't exist, just add the imports for discoverability.
  </action>
  <verify>
Run: `python -c "from src.models import StyleContentType, VocabularyAudit, ConfidenceLevel; print('All exports work')"`
Expected: No import errors
  </verify>
  <done>
New types are importable from src.models package
  </done>
</task>

</tasks>

<verification>
All tasks complete when:
1. `python -c "from src.models.ai import StyleContentType; print(StyleContentType.AI_STYLED.value)"` prints "ai_styled"
2. `python -c "from src.models.vocabulary_audit import VocabularyAudit, ConfidenceLevel, CONFIDENCE_THRESHOLDS; print(ConfidenceLevel.HIGH.value, CONFIDENCE_THRESHOLDS['high'])"` prints "green 0.8"
3. `python -c "from src.models import StyleContentType, VocabularyAudit"` imports without error
</verification>

<success_criteria>
- StyleContentType enum has AI_STYLED, AI_GENERATED, ORIGINAL_NOC (PROV-02)
- VocabularyAudit model tracks NOC terms, non-NOC terms, coverage percentage (PROV-04)
- ConfidenceLevel enum defines green/yellow/red visual indicators (PROV-05 partial)
- CONFIDENCE_THRESHOLDS constants define 0.8/0.5/0.0 boundaries
- All new types exportable from src.models package
</success_criteria>

<output>
After completion, create `.planning/phases/11-provenance-architecture/11-01-SUMMARY.md`
</output>
