---
phase: 10-style-analysis-pipeline
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/services/style_constants.py
  - src/services/few_shot_examples.py
autonomous: true

must_haves:
  truths:
    - "Style rules are available as typed Python constants for runtime prompt construction"
    - "Few-shot examples are available as typed Python data structures"
    - "get_few_shot_prompt() function can build section-specific prompts"
    - "Code constants match documented patterns from Plan 01"
  artifacts:
    - path: "src/services/style_constants.py"
      provides: "Style rules as typed constants"
      contains: "STYLE_RULES"
      exports: ["STYLE_RULES", "KEY_ACTIVITY_VERBS", "SKILLS_PATTERNS", "EFFORT_PATTERNS", "WORKING_CONDITIONS_PATTERNS"]
    - path: "src/services/few_shot_examples.py"
      provides: "Few-shot examples for prompt construction"
      contains: "FEW_SHOT_KEY_ACTIVITIES"
      exports: ["FEW_SHOT_KEY_ACTIVITIES", "FEW_SHOT_SKILLS", "FEW_SHOT_EFFORT", "FEW_SHOT_WORKING_CONDITIONS", "get_few_shot_prompt"]
  key_links:
    - from: "src/services/few_shot_examples.py"
      to: "src/services/style_constants.py"
      via: "Pattern names reference style rule keys"
      pattern: "from.*style_constants.*import|STYLE_RULES"
---

<objective>
Implement style rules and few-shot examples as typed Python constants for runtime prompt construction in Phase 12.

Purpose: The documented patterns from Plan 01 need to be available as code constants so Phase 12's constrained generation can build few-shot prompts programmatically. This translates documentation into executable code.

Output: Two Python modules in `src/services/` - style_constants.py and few_shot_examples.py - with typed data structures and a helper function for prompt building.
</objective>

<execution_context>
@C:\Users\Administrator\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Administrator\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-style-analysis-pipeline/10-CONTEXT.md
@.planning/phases/10-style-analysis-pipeline/10-RESEARCH.md

# Prior plan outputs (REQUIRED - these are the source for code constants)
@.planning/phases/10-style-analysis-pipeline/10-01-SUMMARY.md
@.planning/phases/10-style-analysis-pipeline/10-STYLE-PATTERNS.md
@.planning/phases/10-style-analysis-pipeline/10-FEW-SHOT-EXAMPLES.md

# Existing codebase patterns
@src/services/llm_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Style Constants Module</name>
  <files>src/services/style_constants.py</files>
  <action>
Create `src/services/style_constants.py` with typed constants from 10-STYLE-PATTERNS.md:

1. **Define TypedDict classes** for structure:
```python
from typing import Dict, List, TypedDict

class SectionStyle(TypedDict):
    sentence_structure: str
    verb_tense: str
    perspective: str
    typical_starters: List[str]
    typical_patterns: List[str]
```

2. **Implement section-specific constants:**
   - `KEY_ACTIVITY_VERBS: List[str]` - High-frequency action verbs from corpus
   - `SKILLS_PATTERNS: List[str]` - Pattern templates with placeholders
   - `EFFORT_PATTERNS: List[str]` - Pattern templates with placeholders
   - `WORKING_CONDITIONS_PATTERNS: List[str]` - Pattern templates with placeholders

3. **Implement main style rules dict:**
```python
STYLE_RULES: Dict[str, SectionStyle] = {
    "key_activities": { ... },
    "skills": { ... },
    "effort": { ... },
    "working_conditions": { ... }
}
```

4. **Add docstring with source attribution:**
```python
"""
Style constants for JD writing style transfer.

Source: Corpus analysis from Examples of Job Descriptions/
Patterns documented in: .planning/phases/10-style-analysis-pipeline/10-STYLE-PATTERNS.md
Used by: Phase 12 Constrained Generation for few-shot prompts
"""
```

Translate ALL patterns from 10-STYLE-PATTERNS.md to code - do not omit or summarize. The documentation is the source of truth.
  </action>
  <verify>
- File exists at `src/services/style_constants.py`
- Imports: `from typing import Dict, List, TypedDict`
- Defines: SectionStyle TypedDict
- Exports: STYLE_RULES dict with 4 section keys
- Exports: KEY_ACTIVITY_VERBS list with 15+ verbs
- Exports: SKILLS_PATTERNS, EFFORT_PATTERNS, WORKING_CONDITIONS_PATTERNS lists
- Each SectionStyle has: sentence_structure, verb_tense, perspective, typical_starters, typical_patterns
- Python syntax valid: `python -c "from src.services.style_constants import STYLE_RULES; print(len(STYLE_RULES))"`
  </verify>
  <done>Style rules implemented as typed Python constants matching documented patterns</done>
</task>

<task type="auto">
  <name>Task 2: Implement Few-Shot Examples Module</name>
  <files>src/services/few_shot_examples.py</files>
  <action>
Create `src/services/few_shot_examples.py` with typed examples from 10-FEW-SHOT-EXAMPLES.md:

1. **Define TypedDict for examples:**
```python
from typing import Dict, List, TypedDict

class FewShotExample(TypedDict):
    section: str
    noc_input: str
    styled_output: str
    source_jd: str
    pattern_applied: str
    quality_weight: float
```

2. **Implement section-specific example lists:**
   - `FEW_SHOT_KEY_ACTIVITIES: List[FewShotExample]`
   - `FEW_SHOT_SKILLS: List[FewShotExample]`
   - `FEW_SHOT_EFFORT: List[FewShotExample]`
   - `FEW_SHOT_WORKING_CONDITIONS: List[FewShotExample]`

3. **Implement aggregate dict:**
```python
ALL_FEW_SHOT_EXAMPLES: Dict[str, List[FewShotExample]] = {
    "key_activities": FEW_SHOT_KEY_ACTIVITIES,
    "skills": FEW_SHOT_SKILLS,
    "effort": FEW_SHOT_EFFORT,
    "working_conditions": FEW_SHOT_WORKING_CONDITIONS
}
```

4. **Implement helper function:**
```python
def get_few_shot_prompt(section: str, n_examples: int = 3) -> str:
    """
    Build few-shot prompt section for a given JD element.

    Args:
        section: One of 'key_activities', 'skills', 'effort', 'working_conditions'
        n_examples: Number of examples to include (default 3, max 5 recommended)

    Returns:
        Formatted string with NOC Input / Styled Output pairs
    """
    examples = ALL_FEW_SHOT_EXAMPLES.get(section, [])[:n_examples]

    prompt_parts = []
    for ex in examples:
        prompt_parts.append(f"NOC Input: {ex['noc_input']}")
        prompt_parts.append(f"Styled Output: {ex['styled_output']}")
        prompt_parts.append("")

    return "\n".join(prompt_parts)
```

5. **Add docstring with source attribution:**
```python
"""
Few-shot examples for JD style transfer prompts.

Source: Curated from Examples of Job Descriptions/
Examples documented in: .planning/phases/10-style-analysis-pipeline/10-FEW-SHOT-EXAMPLES.md
Used by: Phase 12 Constrained Generation for few-shot prompt construction
"""
```

Translate ALL examples from 10-FEW-SHOT-EXAMPLES.md to code - every documented example should appear in the corresponding list.
  </action>
  <verify>
- File exists at `src/services/few_shot_examples.py`
- Imports: `from typing import Dict, List, TypedDict`
- Defines: FewShotExample TypedDict
- Exports: FEW_SHOT_KEY_ACTIVITIES, FEW_SHOT_SKILLS, FEW_SHOT_EFFORT, FEW_SHOT_WORKING_CONDITIONS
- Each list has 5-7 examples (matching 10-FEW-SHOT-EXAMPLES.md)
- Each example has all required fields: section, noc_input, styled_output, source_jd, pattern_applied, quality_weight
- Exports: get_few_shot_prompt function
- Python syntax valid: `python -c "from src.services.few_shot_examples import get_few_shot_prompt; print(get_few_shot_prompt('key_activities', 2))"`
  </verify>
  <done>Few-shot examples implemented as typed Python data structures with helper function for prompt building</done>
</task>

<task type="auto">
  <name>Task 3: Verify Module Integration</name>
  <files>src/services/style_constants.py, src/services/few_shot_examples.py</files>
  <action>
Verify both modules work together and are importable:

1. **Run import check:**
```bash
python -c "
from src.services.style_constants import STYLE_RULES, KEY_ACTIVITY_VERBS
from src.services.few_shot_examples import ALL_FEW_SHOT_EXAMPLES, get_few_shot_prompt

# Verify structure
print(f'Style sections: {list(STYLE_RULES.keys())}')
print(f'Verb count: {len(KEY_ACTIVITY_VERBS)}')
print(f'Example sections: {list(ALL_FEW_SHOT_EXAMPLES.keys())}')
for section, examples in ALL_FEW_SHOT_EXAMPLES.items():
    print(f'  {section}: {len(examples)} examples')

# Test helper function
prompt = get_few_shot_prompt('key_activities', 2)
print(f'Sample prompt length: {len(prompt)} chars')
"
```

2. **Verify consistency:**
- All section keys in STYLE_RULES match ALL_FEW_SHOT_EXAMPLES keys
- Example count per section is 5-7 (not more, not less)
- No Python syntax errors

3. **If any errors**, fix the modules and re-verify.

DO NOT add tests to test files - this is a manual verification task. Phase 12 will add proper integration when using these modules.
  </action>
  <verify>
- Both modules import without errors
- STYLE_RULES has 4 keys: key_activities, skills, effort, working_conditions
- ALL_FEW_SHOT_EXAMPLES has same 4 keys
- Each example list has 5-7 items
- get_few_shot_prompt returns non-empty string
  </verify>
  <done>Both modules verified working together, ready for Phase 12 integration</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Both Python modules exist in `src/services/`
2. Modules import without errors
3. Style constants match 10-STYLE-PATTERNS.md documentation
4. Few-shot examples match 10-FEW-SHOT-EXAMPLES.md documentation
5. get_few_shot_prompt function produces formatted prompt text
6. No test files created (Phase 12 handles integration testing)
</verification>

<success_criteria>
- src/services/style_constants.py exports STYLE_RULES and pattern lists
- src/services/few_shot_examples.py exports example lists and get_few_shot_prompt()
- Both modules have TypedDict type annotations
- All documented patterns and examples are translated to code
- Modules work together for prompt construction
- Ready for Phase 12 constrained generation
</success_criteria>

<output>
After completion, create `.planning/phases/10-style-analysis-pipeline/10-02-SUMMARY.md`
</output>
