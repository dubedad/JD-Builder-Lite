---
phase: 29-classification-restyle-generate-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/index.html
  - static/css/classify.css
autonomous: true

must_haves:
  truths:
    - "Classification page shows an occupation header card with gear icon, title, and subtitle"
    - "DADM Compliant (green) and Full Provenance (purple) badges appear in the header card"
    - "TBS 3-step dark navy card shows sections 4.1.1, 4.1.2, 4.1.3"
    - "An orange Analyze button appears below the TBS card"
    - "Post-analysis containers exist in the DOM for result card, alignment comparison, key evidence, caveats, alternatives, and next step"
  artifacts:
    - path: "templates/index.html"
      provides: "Restyled #classify-section with v5.1 chrome structure"
      contains: "classify-header-card"
    - path: "static/css/classify.css"
      provides: "CSS for header card, badges, TBS card, post-analysis sections"
      contains: "classify-header-card"
  key_links:
    - from: "templates/index.html"
      to: "static/css/classify.css"
      via: "CSS class names"
      pattern: "classify-header-card|classify-tbs-card|badge--dadm-compliant"
---

<objective>
Restyle the Classification page (#classify-section) HTML and CSS to match v5.1 chrome.

Purpose: CLASS-01 requires the classification page to render in v5.1 chrome with occupation header card, DADM/Full Provenance badges, and TBS 3-step dark card. This plan replaces the old HTML structure and adds all CSS. It also adds empty DOM containers for post-analysis sections that plan 29-03 will populate via JS.

Output: Restyled #classify-section in index.html + updated classify.css with all new styles.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-classification-restyle-generate-page/29-RESEARCH.md
@templates/index.html
@static/css/classify.css
@static/js/classify.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace #classify-section HTML with v5.1 layout</name>
  <files>templates/index.html</files>
  <action>
Replace the entire `<section id="classify-section" ...>` block (lines ~292-374) with the v5.1 structure below. Preserve ALL existing element IDs that classify.js references (`classify-section`, `classify-loading`, `classify-results`, `classify-error`, `classify-status-badge`, `recommendations-panel`, `evidence-panel`, `jd-text-viewer`, `evidence-close`, `classify-complete`, `classify-position-title`, `classify-stale-warning`, `classify-back-to-edit`, `classify-continue-generate`, `classify-back-to-edit-2`, `classify-continue-generate-2`, `classify-nav-always`).

New HTML structure for #classify-section:

```html
<section id="classify-section" class="classify-section hidden">
  <!-- Occupation Header Card -->
  <div class="classify-header-card">
    <div class="classify-header-card__left">
      <i class="fas fa-cog classify-header-card__icon"></i>
      <div>
        <h2 class="classify-header-card__title">Occupational Group Allocation</h2>
        <p class="classify-header-card__subtitle">Classification Step 1 — Per TBS Directive on Classification</p>
      </div>
    </div>
    <div class="classify-header-card__badges">
      <span class="badge badge--dadm-compliant"><i class="fas fa-check-circle"></i> DADM Compliant</span>
      <span class="badge badge--full-provenance"><i class="fas fa-link"></i> Full Provenance</span>
    </div>
  </div>

  <!-- Position title (populated by JS) -->
  <p id="classify-position-title" class="classify-position-title"></p>

  <!-- Stale warning banner (existing) -->
  <div id="classify-stale-warning" class="classify-stale-warning hidden" role="alert">
    <i class="fas fa-exclamation-triangle"></i>
    <span>JD changed -- re-classify for updated results</span>
    <button onclick="document.getElementById('classify-stale-warning').classList.add('hidden'); window.jdStepper.goToStep(3);" class="btn btn--sm">Re-classify</button>
  </div>

  <!-- TBS 3-Step Dark Card -->
  <div class="classify-tbs-card">
    <div class="classify-tbs-card__step">
      <div class="classify-tbs-card__step-label">§4.1.1</div>
      <div class="classify-tbs-card__step-title">Primary Purpose</div>
      <p class="classify-tbs-card__step-desc">Identify the primary purpose of the position from duties and responsibilities.</p>
    </div>
    <div class="classify-tbs-card__step">
      <div class="classify-tbs-card__step-label">§4.1.2</div>
      <div class="classify-tbs-card__step-title">Definition Fit</div>
      <p class="classify-tbs-card__step-desc">Compare primary purpose against occupational group definitions.</p>
    </div>
    <div class="classify-tbs-card__step">
      <div class="classify-tbs-card__step-label">§4.1.3</div>
      <div class="classify-tbs-card__step-title">Inclusions / Exclusions</div>
      <p class="classify-tbs-card__step-desc">Verify inclusion statements and check for exclusion conflicts.</p>
    </div>
  </div>

  <!-- Status badge (kept for JS compatibility) -->
  <span id="classify-status-badge" class="classify-status-badge hidden">Analyzing...</span>

  <!-- Analyze CTA Button (pre-analysis) -->
  <div id="classify-cta" class="classify-cta">
    <button id="classify-analyze-btn" class="btn btn--cta-orange" type="button">
      <i class="fas fa-pencil-alt"></i> Analyze &amp; Recommend Occupational Group
    </button>
  </div>

  <!-- Loading state -->
  <div id="classify-loading" class="classify-loading hidden">
    <div class="classify-loading-spinner"></div>
    <p>Analyzing job description against TBS occupational group definitions...</p>
  </div>

  <!-- Error state -->
  <div id="classify-error" class="classify-error hidden" role="alert"></div>

  <!-- Results container (populated by JS from plan 29-03) -->
  <div id="classify-results" class="classify-results hidden">
    <!-- Top Result Card (populated by classify.js) -->
    <div id="classify-top-result" class="classify-top-result"></div>

    <!-- Statement Alignment Comparison (CLASS-02) -->
    <div id="classify-alignment" class="classify-alignment hidden">
      <h3 class="classify-section-heading">Statement Alignment Comparison</h3>
      <div class="classify-alignment__columns">
        <div class="classify-alignment__col">
          <h4>Your Selected Statements</h4>
          <ul id="classify-alignment-user" class="classify-alignment__list"></ul>
        </div>
        <div class="classify-alignment__col">
          <h4>OG Definition Statements</h4>
          <ul id="classify-alignment-og" class="classify-alignment__list"></ul>
        </div>
      </div>
      <div id="classify-alignment-score" class="classify-alignment__score"></div>
    </div>

    <!-- Key Evidence (CLASS-03) -->
    <div id="classify-key-evidence" class="classify-key-evidence hidden">
      <h3 class="classify-section-heading">Key Evidence</h3>
      <ul id="classify-evidence-list" class="classify-evidence-bullets"></ul>
    </div>

    <!-- Caveats (CLASS-03) -->
    <div id="classify-caveats" class="classify-caveats hidden">
      <h3 class="classify-section-heading">Caveats</h3>
      <ul id="classify-caveats-list" class="classify-caveats-bullets"></ul>
    </div>

    <!-- Alternative Groups Considered -->
    <div id="classify-alternatives" class="classify-alternatives hidden">
      <h3 class="classify-section-heading">Alternative Groups Considered</h3>
      <div id="classify-alternatives-content"></div>
    </div>

    <!-- Next Step (CLASS-04) -->
    <div id="classify-next-step" class="classify-next-step hidden">
      <h3 class="classify-section-heading">Next Step</h3>
      <div id="classify-next-step-content" class="classify-next-step__box"></div>
    </div>

    <!-- Legacy panels (kept for JS compatibility) -->
    <div class="classify-layout hidden">
      <div id="recommendations-panel" class="recommendations-panel"></div>
      <div id="evidence-panel" class="evidence-panel hidden">
        <header class="evidence-panel-header">
          <h3>Job Description</h3>
          <button id="evidence-close" class="evidence-close" aria-label="Close evidence view"><i class="fas fa-times"></i></button>
        </header>
        <div id="jd-text-viewer" class="jd-text-viewer"></div>
      </div>
    </div>

    <!-- Complete section (kept for JS compatibility) -->
    <div id="classify-complete" class="classify-complete hidden">
      <div class="classify-complete-icon"><i class="fas fa-check-circle"></i></div>
      <h3>Classification Step 1 Complete</h3>
      <p>Review the recommendations above. The occupational group allocation is based on your job description content.</p>
    </div>

    <!-- Always-visible nav actions -->
    <div class="classify-nav-actions classify-nav-actions--always" id="classify-nav-always">
      <button class="btn btn--text" id="classify-back-to-edit-2" type="button">
        <i class="fas fa-arrow-left"></i> Back to Edit
      </button>
      <button class="btn btn--primary" id="classify-continue-generate-2" type="button">
        Continue to Generate <i class="fas fa-arrow-right"></i>
      </button>
    </div>
  </div>
</section>
```

IMPORTANT:
- The `classify-loading` div starts with class `hidden` (was previously visible by default). The classify.js `showLoading()` function removes `hidden`. This is correct because in v5.1, the user sees the TBS card first and clicks Analyze.
- The `classify-cta` div and `classify-analyze-btn` are NEW elements — plan 29-03 will wire the button click to trigger classification.
- Keep `recommendations-panel` and `evidence-panel` in a hidden `.classify-layout` div for backward compatibility with existing classify.js renderRecommendationCards(). Plan 29-03 will transition to new containers.
- Remove the old `classify-header` div, old `classify-status-badge` position, old `classify-nav-actions` inside `classify-complete`, and old heading/subheading elements.
  </action>
  <verify>
Open templates/index.html in a text editor and confirm:
1. `id="classify-section"` exists with the new structure
2. All referenced IDs are present: classify-section, classify-loading, classify-results, classify-error, classify-status-badge, recommendations-panel, evidence-panel, jd-text-viewer, evidence-close, classify-complete, classify-position-title, classify-stale-warning, classify-back-to-edit-2, classify-continue-generate-2, classify-nav-always
3. New IDs exist: classify-header-card, classify-tbs-card, classify-cta, classify-analyze-btn, classify-top-result, classify-alignment, classify-key-evidence, classify-caveats, classify-alternatives, classify-next-step
  </verify>
  <done>
#classify-section contains v5.1 header card with gear icon + title + subtitle, DADM Compliant (green) and Full Provenance (purple) badges, TBS 3-step dark card with sections 4.1.1/4.1.2/4.1.3, orange Analyze CTA button, and empty containers for all post-analysis sections. All existing element IDs preserved for JS compatibility.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for header card, badges, TBS card, and post-analysis section containers</name>
  <files>static/css/classify.css</files>
  <action>
Add the following CSS sections to classify.css. Insert BEFORE the existing SECTION 1 comment block at the top. Keep ALL existing CSS intact (it's still needed for recommendation cards, coaching panel, etc.).

Add these new sections:

**Classify Header Card** (dark navy occupation header, matching build page header card pattern):
```css
.classify-header-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--app-bar-bg, #26374a);
  color: #fff;
  border-radius: 8px;
  padding: 1.25rem 1.5rem;
  margin-bottom: 1.5rem;
}
.classify-header-card__left {
  display: flex;
  align-items: center;
  gap: 1rem;
}
.classify-header-card__icon {
  font-size: 2rem;
  opacity: 0.85;
}
.classify-header-card__title {
  margin: 0;
  font-size: 1.375rem;
  font-weight: 600;
  color: #fff;
}
.classify-header-card__subtitle {
  margin: 0.25rem 0 0;
  font-size: 0.875rem;
  opacity: 0.8;
}
.classify-header-card__badges {
  display: flex;
  gap: 0.5rem;
  flex-shrink: 0;
}
```

**Badge styles** (reusable badge component, matching established .badge pattern):
```css
.badge {
  display: inline-flex;
  align-items: center;
  gap: 0.375rem;
  padding: 0.25rem 0.75rem;
  border-radius: 4px;
  font-size: 0.8125rem;
  font-weight: 500;
  white-space: nowrap;
}
.badge--dadm-compliant {
  background: var(--badge-green-bg, #d4edda);
  color: var(--badge-green-text, #155724);
}
.badge--full-provenance {
  background: var(--pill-oasis-bg, #6f42c1);
  color: #fff;
}
.badge--dadm-level2 {
  background: #fff3e0;
  color: #e65100;
}
.badge--human-review {
  background: #ffebee;
  color: #c62828;
}
.badge--ai-generated {
  background: #e3f2fd;
  color: #1565c0;
  border: 1px solid #90caf9;
}
```

**TBS 3-Step Dark Card:**
```css
.classify-tbs-card {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1px;
  background: rgba(255,255,255,0.15);
  border-radius: 8px;
  overflow: hidden;
  margin-bottom: 1.5rem;
}
.classify-tbs-card__step {
  background: var(--app-bar-bg, #26374a);
  padding: 1.25rem;
  text-align: center;
  color: #fff;
}
.classify-tbs-card__step-label {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  opacity: 0.7;
  margin-bottom: 0.375rem;
}
.classify-tbs-card__step-title {
  font-size: 1rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
}
.classify-tbs-card__step-desc {
  font-size: 0.8125rem;
  line-height: 1.5;
  opacity: 0.8;
  margin: 0;
}
```

**Orange CTA button:**
```css
.classify-cta {
  text-align: center;
  margin-bottom: 2rem;
}
.btn--cta-orange {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: #e67e22;
  color: #fff;
  border: none;
  padding: 0.875rem 2rem;
  font-size: 1.125rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  transition: background 0.2s;
}
.btn--cta-orange:hover {
  background: #d35400;
}
```

**Top Result Card (post-analysis):**
```css
.classify-top-result {
  margin-bottom: 1.5rem;
}
.classify-result-card {
  background: #fff;
  border: 2px solid var(--app-bar-bg, #26374a);
  border-radius: 8px;
  padding: 1.5rem;
}
.classify-result-card__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1rem;
}
.classify-result-card__title {
  font-size: 1.375rem;
  font-weight: 600;
  color: var(--app-bar-bg, #26374a);
  margin: 0;
}
.classify-result-card__subtitle {
  font-size: 0.875rem;
  color: var(--text-light, #6c757d);
  margin: 0.25rem 0 0;
}
.classify-result-card__tbs-link {
  font-size: 0.875rem;
  color: var(--accent, #2b4380);
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
}
.classify-result-card__tbs-link:hover {
  text-decoration: underline;
}
.classify-result-card__confidence-bar {
  height: 8px;
  background: #e0e0e0;
  border-radius: 4px;
  overflow: hidden;
  margin: 0.75rem 0;
}
.classify-result-card__confidence-fill {
  height: 100%;
  border-radius: 4px;
  background: #43a047;
  transition: width 0.3s;
}
.classify-result-card__summary {
  font-size: 0.9375rem;
  line-height: 1.6;
  color: var(--text, #333);
  margin: 0;
}
```

**Post-analysis section containers:**
```css
.classify-section-heading {
  font-size: 1.125rem;
  font-weight: 600;
  color: var(--app-bar-bg, #26374a);
  margin: 0 0 0.75rem;
  padding-bottom: 0.5rem;
  border-bottom: 2px solid #e0e0e0;
}

/* Statement Alignment (CLASS-02) */
.classify-alignment {
  margin-bottom: 1.5rem;
}
.classify-alignment__columns {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
  margin-bottom: 1rem;
}
.classify-alignment__columns h4 {
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--text, #333);
  margin: 0 0 0.5rem;
}
.classify-alignment__list {
  list-style: none;
  padding: 0;
  margin: 0;
}
.classify-alignment__list li {
  padding: 0.5rem 0;
  border-bottom: 1px solid #f0f0f0;
  font-size: 0.875rem;
  line-height: 1.5;
}
.classify-alignment__score {
  font-size: 1rem;
  font-weight: 600;
  color: var(--app-bar-bg, #26374a);
  padding: 0.75rem 1rem;
  background: #f0f4f8;
  border-radius: 6px;
  text-align: center;
}

/* Key Evidence (CLASS-03) - green check bullets */
.classify-key-evidence {
  margin-bottom: 1.5rem;
}
.classify-evidence-bullets {
  list-style: none;
  padding: 0;
  margin: 0;
}
.classify-evidence-bullets li {
  display: flex;
  align-items: flex-start;
  gap: 0.5rem;
  padding: 0.5rem 0;
  font-size: 0.875rem;
  line-height: 1.5;
}
.classify-evidence-bullets li::before {
  content: '\f058'; /* fa-check-circle */
  font-family: 'Font Awesome 6 Free', 'Font Awesome 5 Free';
  font-weight: 900;
  color: #2e7d32;
  flex-shrink: 0;
  margin-top: 0.125rem;
}

/* Caveats (CLASS-03) - amber warning bullets */
.classify-caveats {
  margin-bottom: 1.5rem;
}
.classify-caveats-bullets {
  list-style: none;
  padding: 0;
  margin: 0;
}
.classify-caveats-bullets li {
  display: flex;
  align-items: flex-start;
  gap: 0.5rem;
  padding: 0.5rem 0;
  font-size: 0.875rem;
  line-height: 1.5;
}
.classify-caveats-bullets li::before {
  content: '\f071'; /* fa-exclamation-triangle */
  font-family: 'Font Awesome 6 Free', 'Font Awesome 5 Free';
  font-weight: 900;
  color: #e65100;
  flex-shrink: 0;
  margin-top: 0.125rem;
}

/* Alternative Groups */
.classify-alternatives {
  margin-bottom: 1.5rem;
}

/* Next Step (CLASS-04) */
.classify-next-step {
  margin-bottom: 1.5rem;
}
.classify-next-step__box {
  background: #e3f2fd;
  border: 1px solid #90caf9;
  border-radius: 6px;
  padding: 1.25rem;
  font-size: 0.9375rem;
  line-height: 1.6;
}
.classify-next-step__box a {
  color: var(--accent, #2b4380);
  font-weight: 500;
}
```

**Responsive overrides for TBS card:**
```css
@media (max-width: 768px) {
  .classify-header-card {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.75rem;
  }
  .classify-tbs-card {
    grid-template-columns: 1fr;
  }
  .classify-alignment__columns {
    grid-template-columns: 1fr;
  }
}
```
  </action>
  <verify>
Run the app with `cd /Users/victornishi/Documents/GitHub/JD-Builder2/JD-Builder-Lite && PYTHONPATH=. python3 src/app.py` and navigate to http://127.0.0.1:5000. Load a profile, then click step 3 (Classify) in the stepper. Confirm:
1. Dark navy header card with gear icon, "Occupational Group Allocation" title, subtitle
2. Green "DADM Compliant" badge and purple "Full Provenance" badge in header card
3. TBS 3-step dark card with 3 columns (4.1.1, 4.1.2, 4.1.3)
4. Orange "Analyze & Recommend Occupational Group" button
  </verify>
  <done>
Classification page renders in v5.1 chrome with occupation header card (gear icon, title, subtitle), DADM Compliant (green) and Full Provenance (purple) badges, TBS 3-step dark navy card (4.1.1/4.1.2/4.1.3), and orange analyze CTA. All post-analysis section containers exist in DOM with correct IDs ready for JS population.
  </done>
</task>

</tasks>

<verification>
- Open browser to http://127.0.0.1:5000
- Search for any occupation, select a profile to enter Build
- Click step 3 (Classify) in the stepper
- Verify header card, badges, TBS card, and analyze button are visible
- Verify no JS console errors from classify.js (backward-compatible IDs preserved)
</verification>

<success_criteria>
CLASS-01 fully satisfied: Classification page renders in v5.1 chrome with occupation header card, DADM Compliant (green) and Full Provenance (purple) badges, and TBS 3-step dark card. Post-analysis containers exist in DOM. No JS errors from existing classify.js code.
</success_criteria>

<output>
After completion, create `.planning/phases/29-classification-restyle-generate-page/29-01-SUMMARY.md`
</output>
