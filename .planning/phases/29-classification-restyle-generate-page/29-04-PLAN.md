---
phase: 29-classification-restyle-generate-page
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/ai.py
  - src/services/llm_service.py
  - src/routes/api.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Additional context typed by user is included in the LLM prompt and influences the generated overview"
    - "Generated overview is multi-paragraph prose (3-4 paragraphs), not a single block of 4-6 sentences"
  artifacts:
    - path: "src/models/ai.py"
      provides: "GenerationRequest with additional_context field"
      contains: "additional_context"
    - path: "src/services/llm_service.py"
      provides: "generate_stream and build_user_prompt accept and use additional_context"
      contains: "additional_context"
    - path: "src/routes/api.py"
      provides: "Route passes additional_context to generate_stream"
      contains: "additional_context"
  key_links:
    - from: "src/routes/api.py"
      to: "src/services/llm_service.py"
      via: "generate_stream() call with additional_context kwarg"
      pattern: "generate_stream.*additional_context"
    - from: "src/services/llm_service.py"
      to: "OpenAI API"
      via: "build_user_prompt includes additional_context text"
      pattern: "Additional context"
---

<objective>
Close two verification gaps from Phase 29: (1) wire the additional_context field end-to-end so user-provided context reaches the LLM prompt, and (2) update the system prompt to instruct multi-paragraph output instead of "4-6 sentences."

Purpose: The frontend already sends additional_context but the backend silently discards it (Pydantic v2 ignores unknown fields). The user expects their additional context to influence generation output. The system prompt also needs updating to produce multi-paragraph prose per the GEN-03 requirement.

Output: Three backend files updated; additional_context flows from request through to LLM prompt; system prompt instructs 3-4 paragraphs.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-classification-restyle-generate-page/29-VERIFICATION.md
@src/models/ai.py
@src/services/llm_service.py
@src/routes/api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire additional_context through backend pipeline</name>
  <files>src/models/ai.py, src/services/llm_service.py, src/routes/api.py</files>
  <action>
    Three changes across three files:

    1. `src/models/ai.py` line 42-46 — Add `additional_context` field to `GenerationRequest`:
       ```python
       class GenerationRequest(BaseModel):
           statements: List[StatementInput]
           context: JobContext
           additional_context: str = ""
       ```
       This is an optional string defaulting to empty. Pydantic v2 will now parse the field from the frontend JSON instead of silently ignoring it.

    2. `src/services/llm_service.py`:
       - `build_user_prompt()` (line 71): Add `additional_context: str = ""` parameter. After the occupation_code block (line 88) and before the "Selected NOC Statements:" line, insert a conditional block:
         ```python
         if additional_context:
             parts.extend(["", f"Additional Context from Hiring Manager:", f"{additional_context}", ""])
         ```
       - `generate_stream()` (line 106): Add `additional_context: str = ""` parameter. Pass it to `build_user_prompt()`:
         ```python
         user_prompt = build_user_prompt(statements, context, additional_context)
         ```

    3. `src/routes/api.py` line 317: Pass `additional_context` to `generate_stream()`:
       ```python
       stream_with_context(generate_stream(
           gen_request.statements,
           gen_request.context,
           gen_request.additional_context
       ))
       ```

    Do NOT change the frontend — `static/js/generate.js` already sends `additional_context` in the POST body correctly. Do NOT change `PROMPT_VERSION` yet (that happens in Task 2).
  </action>
  <verify>
    1. `python3 -c "from src.models.ai import GenerationRequest; print(GenerationRequest.model_fields.keys())"` — output includes `additional_context`
    2. `python3 -c "from src.services.llm_service import generate_stream; import inspect; sig = inspect.signature(generate_stream); print(sig)"` — signature shows `additional_context` parameter
    3. `grep -n 'additional_context' src/routes/api.py` — shows the field being passed to generate_stream
  </verify>
  <done>GenerationRequest accepts additional_context, generate_stream() and build_user_prompt() accept and use it, api.py passes it through. The full pipeline from frontend POST body to LLM user prompt is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Update system prompt for multi-paragraph output</name>
  <files>src/services/llm_service.py</files>
  <action>
    In `src/services/llm_service.py`:

    1. Update `build_system_prompt()` (line 57-68): Replace the "4-6 sentences" guideline with multi-paragraph instructions. Change the guidelines to:
       ```
       Guidelines:
       - Write 3-4 paragraphs of professional prose (approximately 200-300 words total)
       - Paragraph 1: Role purpose and organizational context
       - Paragraph 2: Primary responsibilities and key activities
       - Paragraph 3: Required expertise, skills, and working conditions
       - Paragraph 4 (optional): Unique aspects of the role or additional context provided
       - Use professional, clear language appropriate for a Government of Canada job posting
       - Synthesize information from all provided NOC elements
       - Do NOT copy NOC statements verbatim - synthesize and contextualize
       - The overview should give a reader a clear understanding of what this job entails
       ```

    2. Update the trailing instruction in `build_user_prompt()` (line 101): Change:
       `"Generate a professional General Overview section (4-6 sentences) for this job description:"`
       to:
       `"Generate a professional General Overview section (3-4 paragraphs) for this job description:"`

    3. Bump `PROMPT_VERSION` (line 10) from `"v1.0"` to `"v1.1"` to reflect the prompt change for provenance tracking.
  </action>
  <verify>
    1. `grep -n "paragraph" src/services/llm_service.py` — shows "3-4 paragraphs" in both system and user prompt
    2. `grep -n "4-6 sentences" src/services/llm_service.py` — returns NO matches (old wording fully removed)
    3. `grep -n "PROMPT_VERSION" src/services/llm_service.py` — shows `v1.1`
  </verify>
  <done>System prompt instructs 3-4 paragraphs with structured guidance per paragraph. User prompt trailing instruction matches. PROMPT_VERSION bumped to v1.1 for provenance tracking.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from src.models.ai import GenerationRequest; r = GenerationRequest(statements=[], context={'job_title':'Test','noc_code':'1234','noc_title':'Test'}, additional_context='test context'); print(r.additional_context)"` — prints "test context"
2. `python3 -c "from src.services.llm_service import build_user_prompt; print(build_user_prompt([], type('C',(),{'job_title':'Test','noc_code':'1234','noc_title':'Test','occupation_code':None})(), 'department needs Python'))"` — output includes "Additional Context from Hiring Manager:" and "department needs Python"
3. `grep -c "4-6 sentences" src/services/llm_service.py` — returns 0
4. `grep "PROMPT_VERSION" src/services/llm_service.py` — shows v1.1
</verification>

<success_criteria>
- additional_context field exists on GenerationRequest and is parsed from frontend JSON
- generate_stream() accepts additional_context and passes it to build_user_prompt()
- build_user_prompt() includes additional_context in the LLM prompt when non-empty
- api.py route passes gen_request.additional_context to generate_stream()
- System prompt instructs 3-4 paragraphs (not 4-6 sentences)
- PROMPT_VERSION is v1.1
</success_criteria>

<output>
After completion, create `.planning/phases/29-classification-restyle-generate-page/29-04-SUMMARY.md`
</output>
