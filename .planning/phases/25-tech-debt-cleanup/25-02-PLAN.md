---
phase: 25-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/api.py
  - src/services/mapper.py
autonomous: true

must_haves:
  truths:
    - "A stem-in-title match via the OASIS search path returns relevance_score=90, not 85"
    - "_map_working_conditions_enriched() accepts parquet_tabs and sets data_source from work_context parquet tuple the same way _map_effort_enriched() and _map_responsibility_enriched() do"
    - "The call site in mapper.py passes parquet_tabs to _map_working_conditions_enriched()"
  artifacts:
    - path: "src/routes/api.py"
      provides: "OASIS fallback search scoring"
      contains: "relevance_score = 90"
    - path: "src/services/mapper.py"
      provides: "Working conditions mapper with parquet_tabs support"
      contains: "parquet_tabs: Optional[dict] = None"
  key_links:
    - from: "src/routes/api.py"
      to: "search_parquet_reader.py T3 tier"
      via: "Both stem-in-title paths now score 90"
      pattern: "relevance_score = 90"
    - from: "src/services/mapper.py _map_working_conditions_enriched"
      to: "EnrichedJDElementData(data_source=...)"
      via: "parquet_tabs.get('work_context') tuple unpack"
      pattern: "wc_source.*work_context"
---

<objective>
Fix two structural inconsistencies: (1) raise the stem-in-title OASIS search score from 85 to 90 to match the parquet T3 tier, and (2) add parquet_tabs parameter and data_source propagation to _map_working_conditions_enriched() to match _map_effort_enriched() and _map_responsibility_enriched().

Purpose: Search results rank identically regardless of whether they come from the parquet or OASIS path. Working conditions provenance metadata is populated correctly in exported JDs.
Output: src/routes/api.py with score 90 on stem-in-title; src/services/mapper.py with consistent _map_working_conditions_enriched() signature and data_source propagation.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/routes/api.py
@src/services/mapper.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stem-in-title score from 85 to 90 in OASIS search path</name>
  <files>src/routes/api.py</files>
  <action>
    In src/routes/api.py, locate the OASIS fallback scoring block inside the search endpoint. The block assigns relevance scores to results based on title/lead-statement matching. The stem-in-title tier is currently 85 — it must be 90 to match the parquet search reader's T3 tier.

    Line 163 — stem-in-title tier:
      BEFORE: `result.relevance_score = 85`
      AFTER:  `result.relevance_score = 90`

    Context for identification (surrounding lines, do not change these):
      Line 161: `elif title_has_stem:`
      Line 162: `    matched = _find_matched_word(result.title, query_stem)`
      Line 163: `    result.relevance_score = 85`   ← change this only
      Line 164: `    result.match_reason = f"Title contains \"{matched}\""`

    Do NOT change any other score values (100, 95, 60, 50, 10 on the surrounding lines).
    Do NOT change any other lines in the file.
  </action>
  <verify>
    Run: `grep -n "relevance_score = 85" src/routes/api.py` — must return no matches.
    Run: `grep -n "relevance_score = 90" src/routes/api.py` — must return exactly one match on the stem-in-title line.
    Run: `python3 -c "import src.routes.api; print('OK')"` with PYTHONPATH=. — must exit 0.
  </verify>
  <done>Line 163 reads `result.relevance_score = 90`; no other score values changed; file imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add parquet_tabs parameter and data_source to _map_working_conditions_enriched()</name>
  <files>src/services/mapper.py</files>
  <action>
    In src/services/mapper.py, update _map_working_conditions_enriched() to accept parquet_tabs and propagate data_source, mirroring the pattern in _map_effort_enriched() (line 267) and _map_responsibility_enriched() (line 298).

    Step A — Update the method signature on line 329:
      BEFORE: `def _map_working_conditions_enriched(self, data: Dict[str, Any], url: str) -> EnrichedJDElementData:`
      AFTER:  `def _map_working_conditions_enriched(self, data: Dict[str, Any], url: str, parquet_tabs: Optional[dict] = None) -> EnrichedJDElementData:`

    Step B — Add data_source extraction inside the method body, immediately after the docstring line (before the `all_items = ...` line):
      ADD:  `_, wc_source = (parquet_tabs or {}).get("work_context", ([], "oasis"))`
      ADD:  `data_source = wc_source`

    Step C — Add data_source= to the EnrichedJDElementData() return call (currently line 344). The return currently reads:
      BEFORE:
        ```
        return EnrichedJDElementData(
            statements=statements,
            category_definition=guide_csv.get_category_definition('working_conditions'),
            source_attribute="Work Context"
        )
        ```
      AFTER:
        ```
        return EnrichedJDElementData(
            statements=statements,
            category_definition=guide_csv.get_category_definition('working_conditions'),
            source_attribute="Work Context",
            data_source=data_source,
        )
        ```

    Step D — Update the call site on line 131:
      BEFORE: `'working_conditions': self._map_working_conditions_enriched(noc_data, base_url),`
      AFTER:  `'working_conditions': self._map_working_conditions_enriched(noc_data, base_url, parquet_tabs),`

    Note: `Optional` is already imported in mapper.py (used by existing methods). Do NOT add a new import. Do NOT change any other methods.
  </action>
  <verify>
    Run: `python3 -c "from src.services.mapper import mapper; print('OK')"` with PYTHONPATH=. — must exit 0.
    Run: `grep -n "parquet_tabs" src/services/mapper.py` — must include a match on the _map_working_conditions_enriched signature line and the call site line.
    Run: `grep -n "data_source=data_source" src/services/mapper.py` — must show 3 matches (effort, responsibility, working_conditions).
    Run: `grep -n "data_source=wc_source" src/services/mapper.py` — must show 0 (data_source= uses the local variable, not inline).
  </verify>
  <done>_map_working_conditions_enriched() signature includes parquet_tabs: Optional[dict] = None; body extracts wc_source from parquet_tabs; EnrichedJDElementData() includes data_source=data_source; call site on line 131 passes parquet_tabs; file imports cleanly.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -n "relevance_score = 85" src/routes/api.py` returns no matches
2. `grep -n "relevance_score = 90" src/routes/api.py` returns one match (stem-in-title tier)
3. `grep -n "def _map_working_conditions_enriched" src/services/mapper.py` shows `parquet_tabs: Optional[dict] = None` in the signature
4. `grep -c "data_source=data_source" src/services/mapper.py` returns 3 (effort, responsibility, working_conditions)
5. `grep -n "_map_working_conditions_enriched(noc_data, base_url, parquet_tabs)" src/services/mapper.py` returns one match (call site)
6. `python3 -c "from src.services.mapper import NOCMapper; print('OK')"` exits 0
7. `python3 -c "import src.routes.api; print('OK')"` exits 0
</verification>

<success_criteria>
- OASIS stem-in-title search tier scores 90, not 85 — Phase 25 SC-3 met
- _map_working_conditions_enriched() has parquet_tabs parameter and data_source propagation — Phase 25 SC-4 met
- Call site on mapper.py line 131 passes parquet_tabs
- Both files import without errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-tech-debt-cleanup/25-02-SUMMARY.md` following the summary template.
</output>
