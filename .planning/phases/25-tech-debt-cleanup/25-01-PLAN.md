---
phase: 25-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/labels_loader.py
autonomous: true

must_haves:
  truths:
    - "No bare print() calls remain in labels_loader.py on success paths — startup log emits logger.info() lines instead"
    - "No bare print() calls remain in labels_loader.py error handlers — query-level errors emit logger.warning() instead"
    - "All bare except Exception: clauses in query methods log a warning before returning [] — no exception is silently swallowed"
  artifacts:
    - path: "src/services/labels_loader.py"
      provides: "Labels/titles loader with consistent logging"
      contains: "logger.info"
  key_links:
    - from: "src/services/labels_loader.py"
      to: "logging module"
      via: "logger = logging.getLogger(__name__) already present at line 8"
      pattern: "logger\\.info|logger\\.warning"
---

<objective>
Replace bare print() calls in labels_loader.py with proper logger calls and add logger.warning() to bare except Exception: clauses in query methods.

Purpose: Logging consistency — all runtime events use the structured logger so they appear in Flask's log stream alongside other application messages. Silent exceptions in query methods become observable failures.
Output: src/services/labels_loader.py with no print() calls and no silent bare except blocks.
</objective>

<execution_context>
@/Users/victornishi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/victornishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/services/labels_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace print() with logger calls in _load_labels and _load_example_titles</name>
  <files>src/services/labels_loader.py</files>
  <action>
    Replace four print() calls with logger equivalents. The logger variable already exists at line 8: `logger = logging.getLogger(__name__)`.

    Line 163 — success path in _load_labels():
      BEFORE: `print(f"[LabelsLoader] Loaded {len(self._labels_df)} labels from parquet")`
      AFTER:  `logger.info("LabelsLoader: loaded %d labels from parquet", len(self._labels_df))`

    Line 187 — success path in _load_example_titles():
      BEFORE: `print(f"[LabelsLoader] Loaded {len(self._titles_df)} example titles from parquet")`
      AFTER:  `logger.info("LabelsLoader: loaded %d example titles from parquet", len(self._titles_df))`

    Line 221 — except block in get_labels():
      BEFORE: `print(f"[LabelsLoader] Error querying labels: {e}")`
      AFTER:  `logger.warning("LabelsLoader: error querying labels: %s", e)`

    Line 251 — except block in get_example_titles():
      BEFORE: `print(f"[LabelsLoader] Error querying example titles: {e}")`
      AFTER:  `logger.warning("LabelsLoader: error querying example titles: %s", e)`

    Use %-style format strings (not f-strings) to match the existing logger.warning() calls in the same file (see lines 152, 157, 166 etc.).

    Do NOT change any other lines, do NOT import anything (logging is already imported).
  </action>
  <verify>
    Run: `python3 -c "from src.services.labels_loader import labels_loader; print('OK')"` from the project root with PYTHONPATH=.
    Check: `grep -n "print(" src/services/labels_loader.py` — must return no matches on lines 163, 187, 221, 251.
  </verify>
  <done>Lines 163, 187, 221, 251 use logger.info() or logger.warning(); grep for print( in those lines returns nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Add logger.warning() to bare except Exception: clauses in query methods</name>
  <files>src/services/labels_loader.py</files>
  <action>
    Six query methods have bare `except Exception:` clauses that silently `return []`. Add `as e:` capture and a logger.warning() call before the return. This matches the project convention established in Phase 21 (see STATE.md decision: "logger.warning() additive-only").

    The six methods and their current bare except lines (approximate — verify in file):

    get_exclusions() (~line 397):
      BEFORE: `except Exception:`
      AFTER:
        `except Exception as e:`
        `    logger.warning("LabelsLoader: error querying exclusions for %s: %s", oasis_profile_code, e)`

    get_employment_requirements() (~line 414):
      BEFORE: `except Exception:`
      AFTER:
        `except Exception as e:`
        `    logger.warning("LabelsLoader: error querying employment requirements for %s: %s", oasis_profile_code, e)`

    get_workplaces() (~line 431):
      BEFORE: `except Exception:`
      AFTER:
        `except Exception as e:`
        `    logger.warning("LabelsLoader: error querying workplaces for %s: %s", oasis_profile_code, e)`

    get_interests() (~line 471):
      BEFORE: `except Exception:`
      AFTER:
        `except Exception as e:`
        `    logger.warning("LabelsLoader: error querying interests for %s: %s", oasis_profile_code, e)`

    get_personal_attributes() (~line 518):
      BEFORE: `except Exception:`
      AFTER:
        `except Exception as e:`
        `    logger.warning("LabelsLoader: error querying personal attributes for %s: %s", oasis_profile_code, e)`

    get_work_context_filtered() (~line 591):
      BEFORE: `except Exception:`
      AFTER:
        `except Exception as e:`
        `    logger.warning("LabelsLoader: error querying work context for %s (filter=%s): %s", oasis_profile_code, filter_type, e)`

    Note for get_work_context_filtered: the cache_key uses oasis_profile_code and filter_type — use both in the warning. The variable name is `oasis_profile_code` in all methods. Do NOT change the `return []` lines — keep them in place after the new logger.warning() line.

    Do NOT change the _load_* methods — they already use logger.warning() correctly.
  </action>
  <verify>
    Run: `python3 -c "from src.services.labels_loader import labels_loader; print('OK')"` from the project root with PYTHONPATH=.
    Check: `grep -n "except Exception:" src/services/labels_loader.py` — must return no matches (all converted to `except Exception as e:`).
    Check: `grep -c "except Exception as e:" src/services/labels_loader.py` — must be 6 or more.
  </verify>
  <done>All six bare except Exception: clauses are converted to except Exception as e: with a logger.warning() call; the file imports cleanly with no syntax errors.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `python3 -c "from src.services.labels_loader import labels_loader; print('OK')"` exits 0 with output "OK"
2. `grep -n "print(" src/services/labels_loader.py` returns no matches
3. `grep -n "except Exception:" src/services/labels_loader.py` returns no matches
4. `grep -c "except Exception as e:" src/services/labels_loader.py` returns 6 or more
5. `grep -c "logger\." src/services/labels_loader.py` is higher than before (new logger calls present)
</verification>

<success_criteria>
- labels_loader.py success paths use logger.info() not print() — Phase 25 SC-1 met
- All query-method exceptions log a warning before returning [] — Phase 25 SC-2 met
- No bare print() calls remain anywhere in labels_loader.py
- No bare except Exception: clauses remain anywhere in labels_loader.py
- File imports without errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-tech-debt-cleanup/25-01-SUMMARY.md` following the summary template.
</output>
