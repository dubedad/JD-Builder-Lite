---
phase: 07-export-extensions
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/services/export_service.py
  - src/services/pdf_generator.py
  - templates/export/jd_pdf.html
  - templates/export/jd_preview.html
  - static/css/export_print.css
autonomous: true

must_haves:
  truths:
    - "PDF export includes Annex section after Appendix A"
    - "Annex shows all 4 categories with source attribution"
    - "Empty categories display 'No additional information available'"
  artifacts:
    - path: "templates/export/jd_pdf.html"
      provides: "Annex section HTML rendering"
      contains: "Additional Job Information"
    - path: "static/css/export_print.css"
      provides: "Annex section print styles"
      contains: "annex-section"
    - path: "src/services/export_service.py"
      provides: "Annex data integration"
      contains: "annex_data"
  key_links:
    - from: "src/services/export_service.py"
      to: "src/services/annex_builder.py"
      via: "import build_annex_data"
      pattern: "from src\\.services\\.annex_builder import"
    - from: "src/services/pdf_generator.py"
      to: "templates/export/jd_pdf.html"
      via: "render_template"
      pattern: "annex_data=data\\.annex_data"
---

<objective>
Add Annex section to PDF export with all 4 categories and source attribution.

Purpose: OUT-07 requires PDF export to include Annex section with unused NOC reference attributes. This plan integrates the Annex data builder into the export pipeline and renders Annex in PDF template.

Output: PDF exports include "Additional Job Information" Annex after Appendix A with Job Requirements, Career Mobility, Interests (Holland Codes), Personal Suitability (Placement Criteria).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-export-extensions/07-CONTEXT.md
@.planning/phases/07-export-extensions/07-01-SUMMARY.md
@src/services/export_service.py
@src/services/pdf_generator.py
@templates/export/jd_pdf.html
@static/css/export_print.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Annex builder into export service</name>
  <files>src/services/export_service.py</files>
  <action>
Update build_export_data() to include Annex data:

1. Add import at top:
```python
from src.services.annex_builder import build_annex_data
```

2. Modify build_export_data() function:
   - After building compliance_sections, build Annex data
   - Need raw NOC data to identify unused attributes
   - PROBLEM: build_export_data only receives ExportRequest which doesn't have raw NOC data

SOLUTION: Add raw_noc_data parameter to build_export_data (optional, defaults to None):

```python
def build_export_data(request: ExportRequest, raw_noc_data: Dict[str, Any] = None) -> ExportData:
    """
    Build complete export data structure from request.

    Args:
        request: ExportRequest with selections and metadata
        raw_noc_data: Optional raw NOC profile data for Annex generation
    """
    # ... existing code ...

    # Build Annex data if raw NOC data provided
    annex_data = None
    if raw_noc_data:
        annex_data = build_annex_data(
            raw_noc_data=raw_noc_data,
            manager_selections=request.selections,
            noc_code=request.noc_code,
            scraped_at=request.source_metadata.scraped_at
        )

    return ExportData(
        # ... existing fields ...
        annex_data=annex_data
    )
```

3. Update api.py export endpoints to pass raw_noc_data:
   - The frontend sends export request but doesn't have raw NOC data
   - Options: (A) Re-fetch profile in export endpoint, (B) Cache profile in session, (C) Include raw data in export request
   - BEST: Option A - Re-fetch is simple and ensures fresh data

NOTE: This task focuses on export_service.py. The API endpoint update to fetch raw_noc_data will be minimal - add one line to re-fetch before calling build_export_data.
  </action>
  <verify>
```bash
python -c "from src.services.export_service import build_export_data; print('Import OK')"
```
No import errors.
  </verify>
  <done>build_export_data accepts optional raw_noc_data and builds AnnexData when provided</done>
</task>

<task type="auto">
  <name>Task 2: Add Annex section to PDF template and styles</name>
  <files>templates/export/jd_pdf.html, templates/export/jd_preview.html, static/css/export_print.css</files>
  <action>
1. Add to templates/export/jd_pdf.html AFTER the compliance-appendix section (before closing body):

```html
    <!-- Annex: Additional Job Information -->
    {% if annex_data and annex_data.sections %}
    <section class="annex-section">
      <h1>Additional Job Information</h1>
      <p class="annex-intro">
        Reference information from the National Occupational Classification that may be useful
        for recruitment, career development, and job analysis.
      </p>

      {% for section in annex_data.sections %}
      <div class="annex-category">
        <h2>{{ section.title }}</h2>

        {% if not section.items %}
        <p class="annex-empty-state">No additional information available.</p>
        {% else %}

          {% if section.format_type == 'paragraph' %}
          <!-- Job Requirements: paragraphs -->
          {% for item in section.items %}
          <p class="annex-paragraph">{{ item }}</p>
          {% endfor %}

          {% elif section.format_type == 'grouped_list' %}
          <!-- Career Mobility: grouped list with Entry/Advancement headers -->
          {% for item in section.items %}
            {% if item.endswith(':') %}
            <p class="annex-group-header"><strong>{{ item }}</strong></p>
            {% else %}
            <p class="annex-mobility-item">{{ item }}</p>
            {% endif %}
          {% endfor %}

          {% else %}
          <!-- List format: Interests, Personal Suitability -->
          <ul class="annex-list">
            {% for item in section.items %}
            <li>{{ item }}</li>
            {% endfor %}
          </ul>
          {% endif %}

        {% endif %}
      </div>
      {% endfor %}

      <!-- Source attribution -->
      <p class="annex-attribution">
        Source: NOC {{ annex_data.source_noc_code }}, retrieved {{ annex_data.retrieved_at.strftime('%Y-%m-%d') }}
      </p>
    </section>
    {% endif %}
```

2. Add to templates/export/jd_preview.html (same location, after compliance-appendix, before jd-footer):
   - Use same HTML structure as PDF template
   - This ensures preview matches final PDF

3. Add to static/css/export_print.css:

```css
/* Annex Section */
.annex-section {
  page: annex;
  page-break-before: always;
}

@page annex {
  @top-center {
    content: "Annex: Additional Job Information";
    font-family: "Lato", Arial, sans-serif;
    font-size: 10pt;
    color: #26374a;
    padding-bottom: 0.25in;
    border-bottom: 1pt solid #26374a;
  }
}

.annex-section h1 {
  font-size: 18pt;
  color: #26374a;
  text-align: center;
  margin-bottom: 1em;
  page-break-after: avoid;
}

.annex-intro {
  margin-bottom: 1.5em;
  font-style: italic;
}

.annex-category {
  margin-top: 1.5em;
  page-break-inside: avoid;
}

.annex-category h2 {
  font-size: 12pt;
  color: #26374a;
  margin-bottom: 0.75em;
  border-bottom: 1pt solid #cccccc;
  padding-bottom: 0.25em;
  page-break-after: avoid;
}

.annex-empty-state {
  color: #666666;
  font-style: italic;
}

.annex-paragraph {
  margin-bottom: 0.5em;
  text-align: justify;
}

.annex-group-header {
  margin-top: 1em;
  margin-bottom: 0.5em;
}

.annex-mobility-item {
  margin-left: 1em;
  margin-bottom: 0.25em;
}

.annex-list {
  list-style-type: disc;
  margin-left: 1.5em;
}

.annex-list li {
  margin-bottom: 0.5em;
}

.annex-attribution {
  margin-top: 2em;
  font-size: 9pt;
  color: #666666;
  font-style: italic;
  text-align: right;
}
```
  </action>
  <verify>
1. Check template syntax: `python -c "from flask import Flask; app = Flask(__name__, template_folder='templates'); app.jinja_env.get_template('export/jd_pdf.html')"`
2. Check CSS has annex-section class: `grep "annex-section" static/css/export_print.css`
  </verify>
  <done>PDF template renders Annex section with all 4 categories, proper styling, and source attribution</done>
</task>

<task type="auto">
  <name>Task 3: Update API export endpoints to provide raw NOC data</name>
  <files>src/routes/api.py</files>
  <action>
Update export_pdf() and export_docx() endpoints to fetch raw NOC data for Annex generation:

1. In export_pdf() function, after validating export_request:
```python
# Fetch raw NOC data for Annex generation
raw_noc_data = None
try:
    html = scraper.fetch_profile(export_request.noc_code)
    raw_noc_data = parser.parse_profile(html, export_request.noc_code)
except Exception as e:
    current_app.logger.warning(f"Could not fetch NOC data for Annex: {e}")
    # Continue without Annex data - graceful degradation

# Build export data with Annex
export_data = build_export_data(export_request, raw_noc_data)
```

2. Apply same change to export_docx() function

Note: This re-fetches the profile which adds latency (~1-2s). This is acceptable because:
- Export happens once per JD (not repeated)
- Ensures Annex uses fresh NOC data
- Alternative (caching) adds complexity
  </action>
  <verify>
Start Flask app and test PDF export with curl to verify no errors:
```bash
# Test that endpoint still works (actual PDF test requires browser)
curl -X POST "http://localhost:5000/api/export/pdf" \
  -H "Content-Type: application/json" \
  -d '{"noc_code":"72600","job_title":"Test","selections":[],"source_metadata":{"noc_code":"72600","profile_url":"https://test","scraped_at":"2026-01-22T00:00:00Z","version":"2025.0"}}' \
  -o test.pdf && ls -la test.pdf
```
PDF file should be generated (check file size > 0).
  </verify>
  <done>PDF and DOCX export endpoints fetch raw NOC data and pass to build_export_data</done>
</task>

</tasks>

<verification>
1. Export PDF for a profile and verify Annex section appears after Appendix A
2. Annex shows all 4 category headers (Job Requirements, Career Mobility, Interests, Personal Suitability)
3. Empty categories show "No additional information available"
4. Source attribution line appears at bottom of Annex
5. Preview page shows matching Annex content
</verification>

<success_criteria>
- PDF export includes "Additional Job Information" Annex section
- All 4 categories appear in fixed order with proper headings
- Unused NOC attributes appear in correct category sections
- Empty categories display placeholder text (not omitted)
- Source attribution shows NOC code and retrieval date
</success_criteria>

<output>
After completion, create `.planning/phases/07-export-extensions/07-02-SUMMARY.md`
</output>
